# ---------- Godot 4+ ----------
.godot/
/overfit/.godot/
/overfit/android/
# export_presets.cfg 는 커밋한다 — 이게 없으면 익스포트가 남의 기계에서 안 돈다.
# 서명 키스토어 비밀번호 등은 Godot 이 export_credentials.cfg 에 따로 담는다. 그건 커밋하지 않는다.
export_credentials.cfg
*.translation

# Godot 익스포트 산출물
/build/
/builds/
/export/
*.apk
*.aab
*.ipa
*.pck
*.zip
*.dmg
*.exe

# ---------- .NET / C# (Godot mono) ----------
.mono/
bin/
obj/
[Bb]in/
[Oo]bj/
*.user
*.userprefs
*.suo
*.csproj.user
.vs/
*.sln.DotSettings.user

# ---------- 테스트 산출물 ----------
# tools/build.sh test 는 out/TestResults 로 떨어뜨리지만, IDE 테스트 탐색기는 프로젝트 옆에 만든다.
TestResults/
*.trx
*.coverage
coverage.cobertura.xml

# ---------- 빌드 · 검증 산출물 ----------
# 헤드리스 로그 · 스크린샷 · 테스트 결과가 전부 여기로 떨어진다.
out/

# ---------- macOS ----------
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.DocumentRevisions-V100
.fseventsd
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# ---------- Windows ----------
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
desktop.ini
$RECYCLE.BIN/
*.stackdump
*.lnk

# ---------- Linux ----------
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*

# ---------- 에디터 / IDE ----------
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace

# ---------- 기타 ----------
*.log
.env
.env.local

# Claude Code 병렬 작업 워크트리 — 절대 커밋하지 않는다
.claude/worktrees/
# 개인 설정. .claude/settings.json(공유되는 훅 설정)은 커밋한다.
.claude/settings.local.json
