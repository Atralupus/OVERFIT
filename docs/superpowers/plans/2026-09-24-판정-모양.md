# 판정 모양 — 보스전 재설계 1/4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모든 판정을 "몸통 사각형 대 판정 모양(사각형 여러 장)" 으로 바꾸고, 그림에서 판정 모양을 뽑는 도구와 데이터를 세우고, Godot 의 Visible Collision Shapes 로 규칙이 실제로 대 본 판정을 볼 수 있게 한다. 게임은 아직 옛 패턴(변종 아홉)으로 돈다.

**Architecture:** 규칙 층(순수 C#)에 `HitRect` · `HitShape` · `ShapeHit` 를 세우고 `HitResolver` 와 `BattleSim.Strike` 가 그것으로 판정한다. 옛 거리 띠는 좌우 대칭 사각형 두 장으로, 옛 공격 사거리는 높이를 안 보는 사각형 한 장으로 옮겨 그대로 탄다. 판정은 한 틱이 아니라 **창** 동안 살고(옛 패턴은 창 = 한 틱), 규칙은 그 틱에 댄 (모양, 놓은 자리) 를 기록만 해 둔다. 뷰의 `HitboxDebug` 가 그 기록을 `CollisionShape2D` 로 옮겨 Godot 이 그리게 한다.

**Tech Stack:** C# 12 / .NET 8 (규칙 · xUnit v3 + Shouldly), Godot 4.7 mono (뷰), Python 3 표준 라이브러리 (도구), bash (`tools/build.sh`)

**Spec:** `docs/superpowers/specs/2026-09-24-보스전-재설계-design.md` — §3 (판정 모양) · §6.1 (판정 보기) · §7.1 (빗나간 이유) · §10 의 1번

**우산 이슈:** #59

---

## 이 계획의 자리

스펙 §10 의 네 PR 중 **첫 번째**다.

| | | |
|---|---|---|
| **1. 판정 모양** | **이 계획** | 모양 판정 · 도구 · 데이터 · 판정 보기 |
| 2. 플레이어 | 이 계획이 들어간 뒤 쓴다 | 2연격 · ↓ 가드 · K 패리 · 차지 제거 |
| 3. 보스 | 이 계획이 들어간 뒤 쓴다 | 두 패턴 · 프레임 타임라인 · 점프 · 연격 끊김 |
| 4. 연출 | 2·3 뒤에 쓴다 | 링 제거 · 모션 예고 |

2·3 을 지금 쓰지 않는 이유: 둘 다 이 계획의 타입(`HitShape` · `Placement` · 판정 창) 위에 선다. 그 타입이 실제로 들어간 모양을 보고 써야 계획의 코드가 거짓말을 안 한다.

---

## Global Constraints

- **규칙 층은 Godot 을 모른다.** `overfit/battle/rules/**` 에 `using Godot;` 이 없다. 새 순수 파일은 `tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj` 의 링크 목록에 **손으로** 한 줄 넣는다 — 와일드카드 금지.
- **규칙 코드는 TDD** — 실패하는 테스트 먼저. 커밋 훅(`tools/precommit_check.sh`)이 csproj 링크 목록을 읽어 막는다.
- **새 `.cs` 는 `.uid` 짝이 있어야 한다** — `tools/build.sh import` 가 만들고, 같이 커밋한다. `tools/build.sh check` 가 짝을 본다.
- **수치는 데이터에** — `overfit/data/*.json`. C# 에 매직 넘버를 두지 않는다. `required` 키가 빠지면 부팅이 빠진 키를 전부 나열하고 멈춘다.
- **결정성** — 규칙 층에 `System.Random` · 벽시계 금지. `Det` 의 상수와 연산 순서를 건드리지 않는다.
- `Fighter.Tick` 순서는 `Begin → Advance → Move → Fall → Regen` 이다.
- **로그는 `[tag][레벨] key=value`**. `[E]` 는 규칙 위반이고 헤드리스 판정을 떨어뜨린다.
- **Godot 쪽 클래스는 파일 이름 = 클래스 이름**, `public partial class`.
- 빌드 **0 warning / 0 error**. 분석기 전면 억제 금지.
- 커밋 메시지 · 주석은 **한국어**, **왜**를 쓴다. 코드는 주변 코드의 주석 밀도와 관용을 따른다.
- **main 에 직접 커밋하지 않는다.** 커밋 훅은 **명령 문자열**을 본다 — 브랜치 만들기와 커밋은 **다른 셸 호출**로 한다.
- **에셋 PNG 를 커밋하지 않는다.** `git ls-files overfit | grep -c '\.png$'` → **0**.
- 커밋 메시지 끝 줄: `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`
- PR 설명 끝 줄: `🤖 Generated with [Claude Code](https://claude.com/claude-code)`
- 리플레이 골든을 바꾸면 `tools/replay_golden.txt` 주석 블록에 **날짜 · 이슈 · 옛 줄 → 새 줄 · 왜** 를 기존 형식으로 적는다.
- **테스트를 약하게 만들어 통과시키지 않는다.** 셋업이 정말로 움직였으면 셋업을 고치고 단언은 안 고친다 — 그리고 그 사실을 적는다.

## Review Focus

스펙이 암시하지만 어느 태스크의 테스트도 저절로 건드리지 않는 입력 · 조건 중, 사람이 가장 먼저 밟을 다섯이다. 각각의 테스트는 표시된 태스크에 들어 있다.

1. **패턴이 끝나는 바로 그 틱에 선 판정** — 러너가 그 틱에 끝나 `_current` · `CurrentPattern` 이 지워지는데 판정은 그 뒤에 대진다. 관측이 `"?"` 패턴으로 남거나 `NullReferenceException` 이 난다 → **Task 3** `패턴이_끝나는_틱에_선_판정도_제_이름으로_남는다`
2. **대시 무적이 판정 창 중간에 풀린다** — 창이 무적보다 길면 무적이 끝난 뒤 틱에 맞아야 한다(다크소울). 한 틱 판정이었을 때는 일어날 수 없던 일이다 → **Task 3** `대시_무적이_창_중간에_풀리면_그_뒤에_맞는다`
3. **보스가 왼쪽을 본다** — 앞으로만 치는 모양이 등 뒤를 치면 안 된다. 옛 띠는 좌우 대칭이라 이 버그를 한 번도 드러낸 적이 없다 → **Task 1** `보는_쪽이_바뀌면_좌우가_뒤집힌다` · **Task 4** `왼쪽을_보는_보스의_모양은_왼쪽을_친다`
4. **몸이 판정 가장자리에 정확히 닿는다** — 옛 판정은 경계값을 맞은 것으로 쳤다. 뒤집히면 같은 자리가 반 틱마다 다른 답을 낸다 → **Task 1** `가장자리가_닿아도_겹친_것이다`
5. **판정 모양의 기준점이 그림과 어긋난다** — 원본 프레임의 아래끝을 바닥으로 잡으면 보스는 33px, 플레이어는 195px 어긋난다. 테스트는 PNG 없이 돌므로 모양 자체로는 못 잡는다 → **Task 2** `기준점이_그림과_맞는다` (그림에서 따로 잰 값과 한 칸 안인가)

---

## File Structure

| 파일 | 할 일 | 태스크 |
|---|---|---|
| `overfit/battle/rules/HitShape.cs` | **신규** — `HitRect` · `Placement` · `ShapeContact` · `HitShape` · `ShapeHit`. 판정 모양과 그 겹침 | 1 |
| `overfit/battle/rules/HitShapeTable.cs` | **신규** — `HitShapeDef` · `HitShapeTable.Parse`. `hitboxes.json` 을 모양으로 | 2 |
| `overfit/data/hitboxes.json` | **신규 · 커밋** — 도구의 출력 | 2 |
| `tools/extract_hitboxes.py` | **신규** — 그림에서 판정 모양을 뽑는다 | 2 |
| `tools/install_assets.py` | PNG 해독을 RGBA 로도 열게 한다 (`png_rgba`) | 2 |
| `overfit/battle/rules/PatternDef.cs` | `PatternStep.ActiveSeconds` | 3 |
| `overfit/battle/rules/HitBox.cs` | 창 길이 (3) → 띠 네 칸 대신 `HitShape` (4) | 3 · 4 |
| `overfit/battle/rules/PatternRunner.cs` | 창 길이를 싣는다 (3) · `ShapeOf` (4) | 3 · 4 |
| `overfit/battle/rules/BattleSim.cs` | 판정 창(`LiveSwing`) (3) · 모양 판정 (4) · 판정 기록 (5) | 3 · 4 · 5 |
| `overfit/battle/rules/HitResolver.cs` | 몸통 대 모양 · `MissedTooClose` → `MissedByGap` | 4 |
| `overfit/battle/rules/Fighter.cs` | `Body` | 4 |
| `overfit/battle/rules/Boss.cs` | `BossConfig.Height` · `Y` · `Body` | 4 |
| `overfit/data/bosses.json` | `height` 297 | 4 |
| `overfit/battle/rules/PlayerAxes.cs` · `DodgeEvent.cs` | 이름 바꾸기 | 4 |
| `overfit/battle/view/HitboxDebug.cs` | **신규** — 규칙의 사각형을 `CollisionShape2D` 로 | 6 |
| `overfit/battle/Battle.cs` | `HitboxDebug` 를 세우고 매 프레임 넘긴다 | 6 |
| `tools/build.sh` | `HITBOXES=1` → `--debug-collisions` | 6 |
| `tests/.../Battle/HitShapeTests.cs` | **신규** | 1 |
| `tests/.../Battle/HitShapeTableTests.cs` | **신규** | 2 |
| `tests/.../Battle/LiveSwingTests.cs` | **신규** | 3 |
| `tests/.../Battle/HitDebugTests.cs` | **신규** | 5 |
| `tests/.../Battle/TestConfigs.cs` | 시험 패턴 `Sweep` · `SweepSim` | 3 |
| `tests/.../Battle/HitResolverTests.cs` · `PatternRunnerTests.cs` · `BattleSimTests.cs` · `PlayerAxesTests.cs` | 새 시그니처 · 이름 | 4 |

(`tests/...` = `tests/Overfit.Rules.Tests`)

---

## 시작

- [ ] **브랜치를 만든다** (이 셸 호출에서는 커밋하지 않는다)

```bash
cd /Users/songjiwon/Data/Git/Atralupus/PreReLU
git checkout main && git -c credential.helper='!gh auth git-credential' fetch -q https://github.com/Atralupus/OVERFIT.git main && git reset -q --hard FETCH_HEAD
git checkout -b feat/hit-shapes
```

- [ ] **출발점이 초록인지 본다**

Run: `tools/build.sh check`
Expected: `0 Warning(s)` · `0 Error(s)` · `규칙 테스트 통과 — N건` · `.uid 짝 — 전부 있음`. N 을 적어 둔다 — 끝에서 견준다.

Run: `tail -1 tools/replay_golden.txt`
Expected: `51 1840 0 200 25 e552749d39854b0c` — 이것을 **옛 골든**으로 적어 둔다.

---

### Task 1: 판정 모양 — `HitRect` · `HitShape` · `ShapeHit`

**Files:**
- Create: `overfit/battle/rules/HitShape.cs`
- Modify: `tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj` (링크 한 줄)
- Test: `tests/Overfit.Rules.Tests/Battle/HitShapeTests.cs`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `public readonly record struct HitRect(double X0, double X1, double Y0, double Y1)` — `bool Overlaps(HitRect other)` (가장자리 포함)
  - `public readonly record struct Placement(double X, double Y, int Facing)` — 모양을 놓는 자리. `Facing >= 0` 은 오른쪽
  - `public enum ShapeContact { Overlap, TooFar, ByHeight, ByGap }`
  - `public sealed class HitShape` — `HitShape(IReadOnlyList<HitRect> local)` · `IReadOnlyList<HitRect> Local` · `HitRect Bounds` · `static HitShape Band(double minDistance, double maxDistance, double lowHeight, double highHeight)` · `static HitShape Reach(double reach)` · `static HitRect ToWorld(HitRect local, Placement at)` · `IReadOnlyList<HitRect> Place(Placement at)`
  - `public static class ShapeHit` — `static ShapeContact Test(HitShape shape, Placement at, HitRect body)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/Overfit.Rules.Tests/Battle/HitShapeTests.cs`:

```csharp
using System;
using System.Linq;
using Overfit.Battle.Rules;
using Shouldly;
using Xunit;

namespace Overfit.Rules.Tests.Battle;

/// <summary>판정 모양 (이슈 #59 · 설계 §3). 몸통 사각형을 모양에 대 보고, 안 닿으면 왜인지를 말한다.</summary>
public class HitShapeTests
{
    /// <summary>보스 발밑 (1000, 0) 에서 오른쪽을 본다.</summary>
    private static readonly Placement _right = new(1000, 0, 1);

    /// <summary>같은 자리에서 왼쪽을 본다.</summary>
    private static readonly Placement _left = new(1000, 0, -1);

    /// <summary>폭 60 · 키 120 몸통 — 발 중심이 x, 발바닥이 y.</summary>
    private static HitRect Body(double x, double y = 0) => new(x - 30, x + 30, y, y + 120);

    /// <summary>앞으로 100~300, 바닥에서 0~200 한 장.</summary>
    private static HitShape Front() => new(new[] { new HitRect(100, 300, 0, 200) });

    [Fact]
    public void 겹치면_Overlap_이다()
    {
        ShapeHit.Test(Front(), _right, Body(1200)).ShouldBe(ShapeContact.Overlap);
    }

    [Fact]
    public void 보는_쪽이_바뀌면_좌우가_뒤집힌다()
    {
        // 오른쪽을 보면 1100~1300 을 치고, 왼쪽을 보면 700~900 을 친다.
        ShapeHit.Test(Front(), _left, Body(800)).ShouldBe(ShapeContact.Overlap);
        ShapeHit.Test(Front(), _left, Body(1200))
            .ShouldBe(ShapeContact.TooFar, "왼쪽을 보는데 오른쪽 몸이 맞았다 — 뒤집기가 빠졌다");
    }

    [Fact]
    public void 가로_범위_밖이면_TooFar_다()
    {
        ShapeHit.Test(Front(), _right, Body(1400)).ShouldBe(ShapeContact.TooFar);
    }

    [Fact]
    public void 위로_넘으면_ByHeight_다()
    {
        // 판정 윗끝 200 · 발바닥 201 — 한 픽셀 위.
        ShapeHit.Test(Front(), _right, Body(1200, y: 201)).ShouldBe(ShapeContact.ByHeight);
    }

    [Fact]
    public void 초승달_안쪽의_빈_곳은_ByGap_이다()
    {
        // 두 장 사이(−100~100)가 비어 있다. 몸(−30~30)이 거기 서면 외곽 상자 안인데 안 닿는다.
        var crescent = new HitShape(new[]
        {
            new HitRect(-300, -100, 0, 200),
            new HitRect(100, 300, 0, 200),
        });

        ShapeHit.Test(crescent, _right, Body(1000)).ShouldBe(ShapeContact.ByGap);
    }

    [Fact]
    public void 가장자리가_닿아도_겹친_것이다()
    {
        // 판정 앞끝 1300 · 몸 왼끝 1300. 옛 거리 띠가 경계값을 맞은 것으로 쳤던 것을 그대로 잇는다.
        ShapeHit.Test(Front(), _right, Body(1330)).ShouldBe(ShapeContact.Overlap);
    }

    [Fact]
    public void Band_는_좌우_대칭_두_장이다()
    {
        HitShape band = HitShape.Band(50, 250, 0, 340);

        band.Local.Count.ShouldBe(2);
        ShapeHit.Test(band, _right, Body(1150)).ShouldBe(ShapeContact.Overlap);
        ShapeHit.Test(band, _right, Body(850)).ShouldBe(ShapeContact.Overlap, "옛 띠는 좌우를 안 가렸다");
        ShapeHit.Test(band, _right, Body(1000))
            .ShouldBe(ShapeContact.ByGap, "안쪽 50 은 비어 있다 — 옛 안쪽 주머니가 이것이다");
    }

    [Fact]
    public void 몸보다_좁은_틈에는_못_숨는다()
    {
        // 안쪽 20 — 틈 폭 40 이 몸 폭 60 보다 좁다. 옛 판정(몸을 점으로 봤다)에서는 숨을 수 있었다.
        ShapeHit.Test(HitShape.Band(20, 250, 0, 340), _right, Body(1000)).ShouldBe(ShapeContact.Overlap);
    }

    [Fact]
    public void Reach_는_높이를_안_본다()
    {
        // 옛 공격 판정(Strike)은 |dx| − 보스 반폭 ≤ 사거리만 봤다. 높이가 얼마든 같은 답이어야 한다.
        HitShape reach = HitShape.Reach(110);

        ShapeHit.Test(reach, _right, Body(1100, y: 5000)).ShouldBe(ShapeContact.Overlap);
        ShapeHit.Test(reach, _left, Body(1100)).ShouldBe(ShapeContact.Overlap, "옛 판정은 등 뒤도 쳤다");
    }

    [Fact]
    public void 외곽_상자는_모든_사각형을_덮는다()
    {
        var shape = new HitShape(new[] { new HitRect(-120, -20, 40, 90), new HitRect(60, 260, 0, 30) });

        shape.Bounds.ShouldBe(new HitRect(-120, 260, 0, 90));
    }

    [Fact]
    public void 빈_모양은_거절한다()
    {
        Should.Throw<ArgumentException>(() => new HitShape(Array.Empty<HitRect>()));
    }

    [Fact]
    public void 뒤집힌_사각형은_거절한다()
    {
        Should.Throw<ArgumentException>(() => new HitShape(new[] { new HitRect(300, 100, 0, 200) }));
    }

    [Fact]
    public void Place_와_판정이_같은_자리를_가리킨다()
    {
        // 표시는 Place 를, 판정은 ShapeHit 을 쓴다. 둘이 다른 자리를 말하면 디버그 화면이 거짓말을 한다.
        var shape = new HitShape(new[]
        {
            new HitRect(-120, -20, 40, 90),
            new HitRect(60, 260, 0, 30),
            new HitRect(80, 140, 150, 300),
        });

        foreach (Placement at in new[] { _right, _left })
        {
            for (double x = 600; x <= 1400; x += 10)
            {
                for (double y = 0; y <= 320; y += 40)
                {
                    HitRect body = Body(x, y);
                    bool drawn = shape.Place(at).Any(r => r.Overlaps(body));
                    bool ruled = ShapeHit.Test(shape, at, body) == ShapeContact.Overlap;
                    ruled.ShouldBe(drawn, $"facing={at.Facing} x={x} y={y}");
                }
            }
        }
    }
}
```

- [ ] **Step 2: 실패하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~HitShapeTests`
Expected: 빌드 실패 — `error CS0246: The type or namespace name 'HitRect' could not be found`

- [ ] **Step 3: 모양을 쓴다**

`overfit/battle/rules/HitShape.cs`:

```csharp
using System;
using System.Collections.Generic;

namespace Overfit.Battle.Rules;

/// <summary>
/// 축 정렬 사각형. 좌표계는 쓰는 자리가 정한다 — <see cref="HitShape"/> 안에서는 <b>공격자 기준</b>
/// (x 는 몸 중심에서 보는 쪽이 +, y 는 발바닥에서 위로), 월드에서는 아레나 좌표(위가 +)다.
/// </summary>
public readonly record struct HitRect(double X0, double X1, double Y0, double Y1)
{
    /// <summary>
    /// 겹치나. <b>가장자리만 닿아도 겹친 것이다</b> — 옛 판정(거리 띠 · 높이 띠)이 경계값을 맞은 것으로
    /// 쳤고, 모양으로 옮기면서 그 경계를 뒤집지 않는다. 뒤집으면 같은 자리가 반 틱마다 다른 답을 낸다.
    /// </summary>
    public bool Overlaps(HitRect other) =>
        X0 <= other.X1 && other.X0 <= X1 && Y0 <= other.Y1 && other.Y0 <= Y1;
}

/// <summary>판정 모양을 어디에 놓나 — 공격자의 발 중심 · 발바닥 높이 · 보는 쪽.</summary>
/// <param name="X">발 중심 x (월드).</param>
/// <param name="Y">발바닥 높이 (월드, 바닥 0).</param>
/// <param name="Facing">-1 왼쪽 · +1 오른쪽. 0 은 오른쪽으로 친다.</param>
public readonly record struct Placement(double X, double Y, int Facing);

/// <summary>판정 모양이 몸에 어떻게 닿았나. <c>HitResolver</c> 가 이것을 판정 결과로 옮긴다.</summary>
public enum ShapeContact
{
    /// <summary>사각형 하나 이상이 몸과 겹친다.</summary>
    Overlap,

    /// <summary>몸이 모양 전체의 <b>가로 범위 밖</b>이다 — 멀어서.</summary>
    TooFar,

    /// <summary>가로로는 안인데 <b>세로 범위 밖</b>이다 — 넘었거나 아래에 섰다.</summary>
    ByHeight,

    /// <summary>외곽 상자 안인데 <b>빈 칸</b>이다 — 초승달 안쪽 같은 곳. 옛 "안쪽 주머니" 도 여기다.</summary>
    ByGap,
}

/// <summary>
/// 판정 모양 — 공격자 기준 사각형 여러 장 (이슈 #59 · 설계 §3).
///
/// <para>
/// 왜 여러 장인가: 그림의 칼 궤적은 초승달이라 바운딩 박스 하나로 두면 <c>attack3</c> 은 상자의 71% 가
/// 빈 공간이다 — 초승달 한가운데 서 있어도 맞는다. 보이는 것이 곧 맞는 것이어야 하므로 흰 픽셀을 격자로
/// 떠서(<c>tools/extract_hitboxes.py</c>) 줄마다 합친 사각형으로 든다.
/// </para>
/// </summary>
public sealed class HitShape
{
    /// <summary>
    /// <see cref="Reach"/> 의 높이 — "높이를 안 본다" 를 사각형으로 말하는 값이다. 옛 공격 판정(Strike)은
    /// 가로 거리만 봤고, 그림의 모양으로 갈아끼우기(설계 §10 의 2번) 전까지 그 성질을 그대로 옮긴다.
    /// </summary>
    private const double _unbounded = 1_000_000;

    private readonly HitRect[] _local;

    public HitShape(IReadOnlyList<HitRect> local)
    {
        ArgumentNullException.ThrowIfNull(local);
        if (local.Count == 0)
        {
            throw new ArgumentException("빈 판정 모양이다 — 아무것도 안 치는 판정은 판정이 아니다", nameof(local));
        }

        _local = new HitRect[local.Count];
        double x0 = double.PositiveInfinity, x1 = double.NegativeInfinity;
        double y0 = double.PositiveInfinity, y1 = double.NegativeInfinity;
        for (int i = 0; i < local.Count; i++)
        {
            HitRect r = local[i];
            if (r.X0 > r.X1 || r.Y0 > r.Y1)
            {
                throw new ArgumentException($"뒤집힌 사각형 #{i} — {r}", nameof(local));
            }

            _local[i] = r;
            x0 = Math.Min(x0, r.X0);
            x1 = Math.Max(x1, r.X1);
            y0 = Math.Min(y0, r.Y0);
            y1 = Math.Max(y1, r.Y1);
        }

        Bounds = new HitRect(x0, x1, y0, y1);
    }

    /// <summary>공격자 기준 사각형들.</summary>
    public IReadOnlyList<HitRect> Local => _local;

    /// <summary>
    /// 모든 사각형을 덮는 외곽 상자 (공격자 기준). 빗나간 이유(<see cref="ShapeContact"/>)를 가르는 잣대다.
    /// </summary>
    public HitRect Bounds { get; }

    /// <summary>
    /// 옛 거리 띠 · 높이 띠를 모양으로 옮긴다 — <b>좌우 대칭 두 장</b>. 옛 판정은 보스 중심에서의
    /// 거리를 <c>Math.Abs</c> 로 재서 좌우를 안 가렸고, 그 성질을 그대로 옮긴다. 안쪽 끝
    /// (<paramref name="minDistance"/>)이 0 보다 크면 두 장 사이가 빈다 — 옛 "안쪽 주머니" 다.
    /// </summary>
    public static HitShape Band(double minDistance, double maxDistance, double lowHeight, double highHeight) =>
        new(new[]
        {
            new HitRect(minDistance, maxDistance, lowHeight, highHeight),
            new HitRect(-maxDistance, -minDistance, lowHeight, highHeight),
        });

    /// <summary>
    /// 옛 공격 사거리를 모양으로 옮긴다 — 좌우로 <paramref name="reach"/>, 높이는 안 본다.
    /// 옛 판정(<c>|dx| − 보스 반폭 ≤ 사거리</c>)은 몸통 대 사각형으로 옮겨도 가로가 정확히 같다:
    /// 보스 몸통(중심 ± 반폭)이 이 사각형과 겹치는 조건이 바로 그 부등식이다.
    /// </summary>
    public static HitShape Reach(double reach) =>
        new(new[] { new HitRect(-reach, reach, -_unbounded, _unbounded) });

    /// <summary>
    /// 공격자 기준 사각형 하나를 월드로 놓는다. <b>놓는 계산은 여기 하나다</b> — 판정(<see cref="ShapeHit"/>)과
    /// 디버그 표시(<see cref="Place"/>)가 같은 함수를 부르므로 둘이 다른 자리를 가리킬 수 없다.
    /// </summary>
    public static HitRect ToWorld(HitRect local, Placement at) =>
        at.Facing >= 0
            ? new HitRect(at.X + local.X0, at.X + local.X1, at.Y + local.Y0, at.Y + local.Y1)
            : new HitRect(at.X - local.X1, at.X - local.X0, at.Y + local.Y0, at.Y + local.Y1);

    /// <summary>
    /// 월드에 놓은 사각형 전부. <b>디버그 표시용이다</b> — 판정은 이것을 안 부른다
    /// (<see cref="ShapeHit.Test"/> 는 매 틱 목록을 만들지 않고 한 장씩 놓아 본다).
    /// </summary>
    public IReadOnlyList<HitRect> Place(Placement at)
    {
        var placed = new HitRect[_local.Length];
        for (int i = 0; i < _local.Length; i++)
        {
            placed[i] = ToWorld(_local[i], at);
        }

        return placed;
    }
}

/// <summary>판정 모양을 몸에 대 본다. <b>상태가 없다.</b></summary>
public static class ShapeHit
{
    /// <summary>
    /// 몸이 모양에 닿았나, 안 닿았으면 왜인가.
    ///
    /// <para>
    /// 빗나간 이유는 <b>외곽 상자</b>로 가른다 (설계 §7.1): 가로 범위 밖이면 멀어서, 가로로는 안인데
    /// 세로 범위 밖이면 넘어서, 둘 다 안인데 안 닿았으면 틈에 서서다. 가로를 먼저 보는 것은 옛 판정의
    /// 순서(거리 → 높이)를 잇기 위해서다.
    /// </para>
    /// </summary>
    public static ShapeContact Test(HitShape shape, Placement at, HitRect body)
    {
        ArgumentNullException.ThrowIfNull(shape);

        IReadOnlyList<HitRect> local = shape.Local;
        for (int i = 0; i < local.Count; i++)
        {
            if (HitShape.ToWorld(local[i], at).Overlaps(body))
            {
                return ShapeContact.Overlap;
            }
        }

        HitRect bounds = HitShape.ToWorld(shape.Bounds, at);
        if (body.X1 < bounds.X0 || body.X0 > bounds.X1)
        {
            return ShapeContact.TooFar;
        }

        if (body.Y1 < bounds.Y0 || body.Y0 > bounds.Y1)
        {
            return ShapeContact.ByHeight;
        }

        return ShapeContact.ByGap;
    }
}
```

- [ ] **Step 4: 테스트 프로젝트에 링크한다**

`tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj` — `HitBox.cs` 링크 바로 아래에 한 줄:

```xml
    <Compile Include="../../overfit/battle/rules/HitShape.cs" LinkBase="linked/battle/rules" />
```

- [ ] **Step 5: 통과하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~HitShapeTests`
Expected: `Passed! - Failed: 0, Passed: 13`

- [ ] **Step 6: `.uid` 를 만들고 게이트를 돈다**

Run: `tools/build.sh import && ls overfit/battle/rules/HitShape.cs.uid`
Expected: 파일이 있다

Run: `tools/build.sh check`
Expected: 0 warning · 0 error · 규칙 테스트 N+13건 · `.uid 짝 — 전부 있음`

- [ ] **Step 7: 커밋** (새 셸 호출)

```bash
git add overfit/battle/rules/HitShape.cs overfit/battle/rules/HitShape.cs.uid \
  tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj tests/Overfit.Rules.Tests/Battle/HitShapeTests.cs
git commit -m "판정 모양을 세운다 — 사각형 여러 장 · 몸통에 대 보고 왜 안 닿았나까지 (#59)" \
  -m "그림의 칼 궤적은 초승달이라 바운딩 박스 하나로 두면 attack3 은 상자의 71% 가 빈 공간이다. 보이는 것이 곧 맞는 것이어야 하므로 판정을 사각형 여러 장으로 들고, 빗나간 이유(멀어서 · 넘어서 · 틈에 서서)는 외곽 상자로 가른다.

놓는 계산(ToWorld)은 한 곳이다 — 판정과 디버그 표시가 같은 함수를 불러야 표시가 거짓말을 안 한다. 옛 거리 띠는 좌우 대칭 두 장(Band), 옛 공격 사거리는 높이를 안 보는 한 장(Reach)으로 옮길 자리를 같이 둔다." \
  -m "Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 판정 데이터 — `hitboxes.json` · `extract_hitboxes.py` · `HitShapeTable`

**Files:**
- Create: `overfit/battle/rules/HitShapeTable.cs`
- Create: `overfit/data/hitboxes.json`
- Create: `tools/extract_hitboxes.py`
- Modify: `tools/install_assets.py` (`png_rgba` 를 떼어 내고 `png_alpha_rows` 가 그것을 쓴다)
- Modify: `tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj` (링크 한 줄)
- Test: `tests/Overfit.Rules.Tests/Battle/HitShapeTableTests.cs`

**Interfaces:**
- Consumes: Task 1 의 `HitRect` · `HitShape`
- Produces:
  - `public sealed class HitShapeDef` — `required double Scale` · `required IReadOnlyList<double> Region` · `required IReadOnlyList<IReadOnlyList<double>> Rects`
  - `public static class HitShapeTable` — `static Dictionary<string, HitShape> Parse(string json, string source)` · 문제는 전부 모아 `DataException`
  - `overfit/data/hitboxes.json` 의 id 다섯: `medieval_king/attack/2` · `medieval_king/attack2/2` · `medieval_king/attack3/2` · `martial_hero/attack/4` · `martial_hero/attack2/4`
  - `tools/install_assets.py` 의 `png_rgba(path) -> (width, height, rows)` — `rows[y]` 는 필터를 푼 RGBA `bytearray`

> 원본 PNG 가 `overfit/assets/` 에 설치돼 있어야 도구가 돈다. 없으면 `python3 tools/install_assets.py` 를 먼저 돌린다 (받아둔 zip 이 `~/Downloads` 에 있어야 한다). **PNG 는 커밋하지 않는다** — 도구의 출력(`hitboxes.json`)만 커밋한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/Overfit.Rules.Tests/Battle/HitShapeTableTests.cs`:

```csharp
using System.IO;
using Overfit.Battle.Rules;
using Overfit.Core;
using Shouldly;
using Xunit;

namespace Overfit.Rules.Tests.Battle;

/// <summary>
/// 판정 데이터 (이슈 #59 · 설계 §3.3). <c>hitboxes.json</c> 은 도구(<c>tools/extract_hitboxes.py</c>)가 쓰고
/// 사람은 안 고친다 — 여기서 보는 것은 <b>도구를 돌렸나</b>와 <b>그림과 같은 기준으로 돌렸나</b>다.
/// </summary>
public class HitShapeTableTests
{
    private static readonly string[] _ids =
    {
        "medieval_king/attack/2",
        "medieval_king/attack2/2",
        "medieval_king/attack3/2",
        "martial_hero/attack/4",
        "martial_hero/attack2/4",
    };

    private static string Json() => File.ReadAllText(Path.Combine("data", "hitboxes.json"));

    [Fact]
    public void 실제_hitboxes_json_을_읽는다()
    {
        var shapes = HitShapeTable.Parse(Json(), "hitboxes.json");

        foreach (string id in _ids)
        {
            shapes.ShouldContainKey(id);
            shapes[id].Local.Count.ShouldBeGreaterThan(0, $"{id} 가 비었다 — 도구를 돌렸나");
        }
    }

    [Fact]
    public void 배율은_balance_json_과_같다()
    {
        // 판정 크기와 그려지는 크기가 따로 놀면 "보이는 것이 곧 맞는 것" 이 무너진다.
        // 어느 팩이 보스이고 파이터인지는 bosses.json · fighters.json 의 sprite 가 말한다.
        BalanceData balance = TestConfigs.Balance();
        string bossSprite = TestConfigs.Bosses()[balance.Battle.Boss].Sprite;
        string fighterSprite = TestConfigs.Fighters()[balance.Battle.Fighter].Sprite;
        var defs = JsonData<HitShapeDef>.ParseTable(Json(), "hitboxes.json");

        foreach ((string id, HitShapeDef def) in defs)
        {
            string pack = id.Split('/')[0];
            double expected = pack == bossSprite ? balance.Feel.BossSpriteScale
                : pack == fighterSprite ? balance.Feel.FighterSpriteScale
                : double.NaN;
            expected.ShouldNotBe(double.NaN, $"{id}: {pack} 는 보스도 파이터도 아니다 — 배율을 모른다");
            def.Scale.ShouldBe(expected, $"{id}");
        }
    }

    /// <summary>
    /// 기준점이 그림과 맞나. 아래 값은 도구가 아니라 <b>그림에서 따로 잰</b> 흰 궤적의 끝이다
    /// (region 기준 · 발바닥 = region 아래끝 · 몸 중심 = region 가로 가운데 — 2026-09-24 PIL 로 잼).
    /// 도구의 칸(4px)만큼은 어긋날 수 있으므로 한 칸 안이면 맞은 것이다.
    ///
    /// <para>
    /// 이 테스트가 잡는 것: 원본 프레임의 아래끝을 바닥으로 잡으면 보스는 33px, 플레이어는 195px 어긋난다.
    /// 가로 가운데를 빼먹으면 수백 px 이다. 테스트는 PNG 없이 돌므로 이렇게 따로 잰 값과 견주는 것 말고는
    /// 기준점을 볼 방법이 없다.
    /// </para>
    /// </summary>
    [Theory]
    [InlineData("medieval_king/attack/2", -110.0, 390.5, 49.5, 236.5, 22.5)]
    [InlineData("martial_hero/attack/4", -45.0, 222.5, 10.0, 172.5, 10.5)]
    public void 기준점이_그림과_맞는다(string id, double x0, double x1, double y0, double y1, double oneCell)
    {
        HitRect bounds = HitShapeTable.Parse(Json(), "hitboxes.json")[id].Bounds;

        bounds.X0.ShouldBe(x0, oneCell, $"{id} 앞뒤가 어긋났다 — 몸 중심을 region 가운데로 잡았나");
        bounds.X1.ShouldBe(x1, oneCell, $"{id} 앞뒤가 어긋났다 — 몸 중심을 region 가운데로 잡았나");
        bounds.Y0.ShouldBe(y0, oneCell, $"{id} 높이가 어긋났다 — 발바닥을 region 아래끝으로 잡았나");
        bounds.Y1.ShouldBe(y1, oneCell, $"{id} 높이가 어긋났다 — 발바닥을 region 아래끝으로 잡았나");
    }

    [Fact]
    public void 사각형이_없는_칸은_거절한다()
    {
        const string json = """{ "a/b/1": { "scale": 1, "region": [0, 0, 1, 1], "rects": [] } }""";

        Should.Throw<DataException>(() => HitShapeTable.Parse(json, "t.json")).Message.ShouldContain("a/b/1");
    }

    [Fact]
    public void 사각형은_네_숫자여야_한다()
    {
        const string json = """{ "a/b/1": { "scale": 1, "region": [0, 0, 1, 1], "rects": [[0, 1, 2]] } }""";

        Should.Throw<DataException>(() => HitShapeTable.Parse(json, "t.json")).Message.ShouldContain("a/b/1");
    }

    [Fact]
    public void 뒤집힌_사각형은_거절한다()
    {
        const string json = """{ "a/b/1": { "scale": 1, "region": [0, 0, 1, 1], "rects": [[5, 1, 0, 1]] } }""";

        Should.Throw<DataException>(() => HitShapeTable.Parse(json, "t.json")).Message.ShouldContain("a/b/1");
    }
}
```

- [ ] **Step 2: 실패하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~HitShapeTableTests`
Expected: 빌드 실패 — `error CS0246: The type or namespace name 'HitShapeTable' could not be found`

- [ ] **Step 3: 로더를 쓴다**

`overfit/battle/rules/HitShapeTable.cs`:

```csharp
using System;
using System.Collections.Generic;
using Overfit.Core;

namespace Overfit.Battle.Rules;

/// <summary>
/// <c>hitboxes.json</c> 의 한 칸. <b>도구(<c>tools/extract_hitboxes.py</c>)가 쓰고 사람은 안 고친다</b> —
/// 판정 모양은 그림에서 뽑는 것이지 손으로 적는 것이 아니다 (이슈 #59 · 설계 §3).
/// </summary>
public sealed class HitShapeDef
{
    /// <summary>그림의 배율. <c>balance.json</c> 의 <c>feel.*_sprite_scale</c> 과 같아야 한다 — 테스트가 본다.</summary>
    public required double Scale { get; init; }

    /// <summary><c>.tres</c> 의 region — [x, y, 폭, 높이]. 어느 픽셀에서 뽑았는지를 남긴다.</summary>
    public required IReadOnlyList<double> Region { get; init; }

    /// <summary>공격자 기준 사각형들 — 각각 [x0, x1, y0, y1] (월드 px).</summary>
    public required IReadOnlyList<IReadOnlyList<double>> Rects { get; init; }
}

/// <summary><c>hitboxes.json</c> → 판정 모양. 문제는 <b>전부</b> 모아 한 번에 던진다 — 부팅이 빠진 키를 전부 나열하는 것과 같은 규약이다.</summary>
public static class HitShapeTable
{
    public static Dictionary<string, HitShape> Parse(string json, string source)
    {
        Dictionary<string, HitShapeDef> defs = JsonData<HitShapeDef>.ParseTable(json, source);
        var shapes = new Dictionary<string, HitShape>(defs.Count);
        var problems = new List<string>();

        foreach ((string id, HitShapeDef def) in defs)
        {
            var rects = new List<HitRect>(def.Rects.Count);
            bool broken = false;
            foreach (IReadOnlyList<double> r in def.Rects)
            {
                if (r.Count != 4)
                {
                    problems.Add($"{id}: 사각형은 [x0, x1, y0, y1] 넷이어야 한다 — {r.Count}개");
                    broken = true;
                    continue;
                }

                rects.Add(new HitRect(r[0], r[1], r[2], r[3]));
            }

            if (broken)
            {
                continue;
            }

            if (rects.Count == 0)
            {
                problems.Add($"{id}: 사각형이 없다 — tools/extract_hitboxes.py 를 돌렸나");
                continue;
            }

            try
            {
                shapes[id] = new HitShape(rects);
            }
            catch (ArgumentException e)
            {
                problems.Add($"{id}: {e.Message}");
            }
        }

        if (problems.Count > 0)
        {
            throw new DataException($"{source}: {string.Join(" / ", problems)}");
        }

        return shapes;
    }
}
```

- [ ] **Step 4: 링크한다**

`tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj` — `HitShape.cs` 링크 바로 아래:

```xml
    <Compile Include="../../overfit/battle/rules/HitShapeTable.cs" LinkBase="linked/battle/rules" />
```

- [ ] **Step 5: 데이터 파일의 뼈대를 만든다** — 키가 곧 뽑을 목록이다. 사각형은 아직 비어 있다

`overfit/data/hitboxes.json`:

```json
{
  "_comment": "판정 모양 (이슈 #59 · 설계 §3). **tools/extract_hitboxes.py 가 쓴다 — 손으로 고치지 않는다.** 키가 곧 뽑을 목록이고(팩/애니메이션/프레임, 애니메이션 이름은 .tres 의 이름), 새 모양이 필요하면 키를 더하고 도구를 돌린다. 좌표는 공격자 기준 월드 px — x 는 몸 중심에서 보는 쪽이 +, y 는 발바닥에서 위로. 기준점은 .tres 의 region 이다: 아래끝이 발바닥, 가로 가운데가 몸 중심.",
  "_source": { "cell_px": 4, "white_min": 235, "alpha_min": 200, "fill_min": 0.25 },
  "martial_hero/attack/4": { "scale": 0, "region": [0, 0, 0, 0], "rects": [] },
  "martial_hero/attack2/4": { "scale": 0, "region": [0, 0, 0, 0], "rects": [] },
  "medieval_king/attack/2": { "scale": 0, "region": [0, 0, 0, 0], "rects": [] },
  "medieval_king/attack2/2": { "scale": 0, "region": [0, 0, 0, 0], "rects": [] },
  "medieval_king/attack3/2": { "scale": 0, "region": [0, 0, 0, 0], "rects": [] }
}
```

Run: `tools/build.sh test --filter FullyQualifiedName~HitShapeTableTests`
Expected: 컴파일은 되고 **넷이 FAIL** — `실제_hitboxes_json_을_읽는다` 와 `기준점이_그림과_맞는다` 두 칸은 `DataException ... 사각형이 없다 — tools/extract_hitboxes.py 를 돌렸나`, `배율은_balance_json_과_같다` 는 배율 0. 거절 테스트 셋은 이미 통과한다.

- [ ] **Step 6: PNG 해독기를 RGBA 로도 열게 한다** — 두 도구가 해독기 하나를 쓴다

먼저 지금 해독기가 내는 값을 적어 둔다 (고친 뒤 견준다):

```bash
python3 - <<'PY' > out/alpha_before.txt
import sys, pathlib; sys.path.insert(0, "tools"); import install_assets
for p in sorted(pathlib.Path("overfit/assets").rglob("*.png")):
    print(p, install_assets.png_alpha_rows(p))
PY
wc -l out/alpha_before.txt
```
Expected: 시트 수만큼 줄이 나온다 (20줄 안팎)

`tools/install_assets.py` — `def png_alpha_rows(path):` 함수 **전체**를 아래 두 함수로 바꾼다:

```python
def png_rgba(path):
    """(폭, 높이, 행 목록). 행은 필터를 푼 RGBA 바이트다.

    판독기가 하나인 이유: 이 파일(발 높이를 재 region 을 자른다)과 extract_hitboxes.py(판정 모양을 뽑는다)가
    같은 픽셀을 봐야 판정이 그려지는 자리와 맞는다. 판독기가 둘이면 한쪽만 고쳐지는 날이 온다.
    """
    data = path.read_bytes()
    if data[:8] != b"\x89PNG\r\n\x1a\x0a":
        raise ValueError(f"PNG 가 아닙니다: {path}")

    width = height = depth = color = interlace = 0
    pixels = bytearray()
    pos = 8
    while pos + 8 <= len(data):
        length = int.from_bytes(data[pos:pos + 4], "big")
        kind = data[pos + 4:pos + 8]
        body = data[pos + 8:pos + 8 + length]
        if kind == b"IHDR":
            width, height, depth, color, _, _, interlace = struct.unpack(">IIBBBBB", body)
        elif kind == b"IDAT":
            pixels += body
        elif kind == b"IEND":
            break
        pos += 12 + length

    if (depth, color, interlace) != (8, 6, 0):
        raise ValueError(
            f"{path}: 8bit RGBA · 비인터레이스만 읽습니다 "
            f"(depth={depth} color={color} interlace={interlace}). "
            "팩이 바뀌었다면 이 판독기를 늘리세요 — 조용히 건너뛰면 발 높이가 틀어집니다.")

    raw = zlib.decompress(bytes(pixels))
    bpp, stride = 4, width * 4
    prev = bytearray(stride)
    rows = []
    at = 0
    for _ in range(height):
        kind = raw[at]
        at += 1
        line = bytearray(raw[at:at + stride])
        at += stride
        _unfilter(kind, line, prev, bpp, stride)
        rows.append(line)
        prev = line

    return width, height, rows


def png_alpha_rows(path):
    """(폭, 높이, 알파가 있는 첫 행, 마지막 행). 전부 투명하면 두 행은 None 이다."""
    width, height, rows = png_rgba(path)
    first = last = None
    for y, line in enumerate(rows):
        if any(line[3::4]):
            first = y if first is None else first
            last = y
    return width, height, first, last
```

고친 뒤 같은 값을 내는지 본다:

```bash
python3 - <<'PY' > out/alpha_after.txt
import sys, pathlib; sys.path.insert(0, "tools"); import install_assets
for p in sorted(pathlib.Path("overfit/assets").rglob("*.png")):
    print(p, install_assets.png_alpha_rows(p))
PY
diff out/alpha_before.txt out/alpha_after.txt && echo "같다"
```
Expected: `같다`

- [ ] **Step 7: 도구를 쓴다**

`tools/extract_hitboxes.py`:

```python
#!/usr/bin/env python3
"""판정 모양을 그림에서 뽑아 overfit/data/hitboxes.json 에 쓴다 (이슈 #59 · 설계 §3).

    python3 tools/extract_hitboxes.py           뽑아서 쓴다
    python3 tools/extract_hitboxes.py --check   뽑은 것이 파일과 같은지만 본다 (다르면 1)

무엇을 뽑나 — hitboxes.json 의 **키가 곧 목록**이다. 키는 `팩/애니메이션/프레임` 이고
(예: `medieval_king/attack/2`), 새 판정 모양이 필요하면 키를 하나 더하고 이 도구를 돌린다.
애니메이션 이름은 **.tres 의 이름**이다 — 시트 파일 이름(attack1.png)이 아니라 뷰가 부르는 이름(attack).

어디를 기준으로 재나 — **.tres 의 region** 이다. install_assets.py 가 팩 전체의 불투명 범위로
세로를 잘라 두었고(그 아래끝이 곧 발바닥이다) 뷰는 그 region 을 그린다. 원본 프레임의 아래끝을
바닥으로 잡으면 보스는 33px, 파이터는 195px 어긋난 자리에 판정이 선다. 가로는 region 의 가운데가
몸 중심이다 (AnimatedSprite2D 는 가운데 정렬이다).

어떻게 뜨나 — region 을 cell_px 칸으로 나눠, 칸 안의 흰 픽셀(불투명도 > alpha_min, R·G·B 전부 > white_min)이
fill_min 이상이면 그 칸을 판정으로 친다. 같은 줄에서 이어진 칸은 가로로 합쳐 사각형 하나로 만든다.
네 값은 hitboxes.json 의 `_source` 에 있다 — 수치는 데이터에 둔다.

배율은 balance.json 의 feel.boss_sprite_scale · fighter_sprite_scale 이다. 어느 팩이 보스이고
파이터인지는 bosses.json · fighters.json 의 sprite 가 말한다.

그림 원본(PNG)은 저장소에 없다 — install_assets.py 를 먼저 돌린다. 이 도구의 출력만 커밋한다.
"""

import argparse
import json
import math
import pathlib
import re
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))
import install_assets  # noqa: E402 — PNG 판독기를 같이 쓴다 (Pillow 를 요구하지 않는다)

ROOT = pathlib.Path(__file__).resolve().parent.parent
DATA = ROOT / "overfit" / "data"
FRAMES = ROOT / "overfit" / "assets" / "spriteframes"
OUT = DATA / "hitboxes.json"

EXT_RE = re.compile(r'^\[ext_resource type="Texture2D" path="(?P<path>[^"]+)" id="(?P<id>[^"]+)"\]$')
SUB_RE = re.compile(r'^\[sub_resource type="AtlasTexture" id="(?P<id>[^"]+)"\]$')
ATLAS_RE = re.compile(r'^atlas = ExtResource\("(?P<id>[^"]+)"\)$')
REGION_RE = re.compile(r'^region = Rect2\((?P<x>[\d.]+), (?P<y>[\d.]+), (?P<w>[\d.]+), (?P<h>[\d.]+)\)$')


def read_json(path):
    return json.loads(path.read_text(encoding="utf-8"))


def scales():
    """팩 이름 → 배율. 역할(보스 · 파이터)은 데이터의 sprite 가 정한다."""
    feel = read_json(DATA / "balance.json")["feel"]
    table = {}
    for key, boss in read_json(DATA / "bosses.json").items():
        if not key.startswith("_"):
            table[boss["sprite"]] = feel["boss_sprite_scale"]
    for key, fighter in read_json(DATA / "fighters.json").items():
        if not key.startswith("_"):
            table[fighter["sprite"]] = feel["fighter_sprite_scale"]
    return table


def atlas(pack):
    """.tres 를 읽어 `애니메이션_프레임` → {"sheet": 시트 경로, "region": (x, y, 폭, 높이)}."""
    ext, subs, current = {}, {}, None
    for line in (FRAMES / f"{pack}.tres").read_text(encoding="utf-8").splitlines():
        if m := EXT_RE.match(line):
            ext[m["id"]] = ROOT / "overfit" / m["path"].removeprefix("res://")
        elif m := SUB_RE.match(line):
            current = {}
            subs[m["id"]] = current
        elif current is not None and (m := ATLAS_RE.match(line)):
            current["sheet"] = ext[m["id"]]
        elif current is not None and (m := REGION_RE.match(line)):
            current["region"] = tuple(int(float(m[k])) for k in ("x", "y", "w", "h"))
    return subs


def rects_of(sheet, region, scale, source):
    """region 안의 흰 픽셀을 칸으로 떠서 줄마다 합친 사각형들 — 공격자 기준 월드 px."""
    cell = source["cell_px"]
    _, _, rows = install_assets.png_rgba(sheet)
    rx, ry, rw, rh = region
    cols = math.ceil(rw / cell)
    lines = math.ceil(rh / cell)

    def white(x, y):
        o = x * 4
        r, g, b, a = rows[y][o:o + 4]
        return a > source["alpha_min"] and min(r, g, b) > source["white_min"]

    out = []
    for cy in range(lines):
        y_top, y_bot = cy * cell, min((cy + 1) * cell, rh)
        run = None
        for cx in range(cols + 1):   # 한 칸 더 — 줄 끝에서 달리던 것을 닫는다
            filled = False
            if cx < cols:
                x_left, x_right = cx * cell, min((cx + 1) * cell, rw)
                hits = sum(
                    white(rx + x, ry + y)
                    for y in range(y_top, y_bot) for x in range(x_left, x_right))
                filled = hits >= source["fill_min"] * (x_right - x_left) * (y_bot - y_top)
            if filled and run is None:
                run = cx
            elif not filled and run is not None:
                x0 = (run * cell - rw / 2) * scale
                x1 = (min(cx * cell, rw) - rw / 2) * scale
                y0 = (rh - y_bot) * scale
                y1 = (rh - y_top) * scale
                out.append([round(x0, 2), round(x1, 2), round(y0, 2), round(y1, 2)])
                run = None
    return out


def extract(existing):
    """지금 파일의 키마다 다시 뽑는다. 메타 키(_로 시작)는 그대로 둔다."""
    source = existing["_source"]
    table = scales()
    out = {k: v for k, v in existing.items() if k.startswith("_")}
    for key in sorted(k for k in existing if not k.startswith("_")):
        pack, anim, frame = key.split("/")
        sub = atlas(pack).get(f"{anim}_{frame}")
        if sub is None:
            raise SystemExit(f"{key}: {pack}.tres 에 {anim}_{frame} 가 없다 — 애니메이션 이름은 .tres 의 이름이다")
        if pack not in table:
            raise SystemExit(f"{key}: {pack} 는 bosses.json · fighters.json 어느 sprite 도 아니다 — 배율을 모른다")
        rects = rects_of(sub["sheet"], sub["region"], table[pack], source)
        if not rects:
            raise SystemExit(f"{key}: 흰 픽셀이 없다 — 판정 프레임이 맞나")
        out[key] = {"scale": table[pack], "region": list(sub["region"]), "rects": rects}
    return out


def dump(data):
    """사람이 읽는 모양 — 사각형 한 줄에 하나. 같은 입력이면 같은 글자다."""
    lines = ["{"]
    items = list(data.items())
    for i, (key, value) in enumerate(items):
        comma = "," if i < len(items) - 1 else ""
        if key.startswith("_"):
            lines.append(f"  {json.dumps(key)}: {json.dumps(value, ensure_ascii=False)}{comma}")
            continue
        lines.append(f"  {json.dumps(key)}: {{")
        lines.append(f'    "scale": {value["scale"]}, "region": {json.dumps(value["region"])},')
        lines.append('    "rects": [')
        rects = value["rects"]
        for j, r in enumerate(rects):
            lines.append(f"      {json.dumps(r)}{',' if j < len(rects) - 1 else ''}")
        lines.append("    ]")
        lines.append(f"  }}{comma}")
    lines.append("}")
    return "\n".join(lines) + "\n"


def main():
    parser = argparse.ArgumentParser(description=__doc__.splitlines()[0])
    parser.add_argument("--check", action="store_true", help="뽑은 것이 파일과 같은지만 본다")
    args = parser.parse_args()

    data = extract(read_json(OUT))
    text = dump(data)
    if args.check:
        if OUT.read_text(encoding="utf-8") != text:
            print("hitboxes.json 이 그림과 다르다 — python3 tools/extract_hitboxes.py 를 돌려라")
            return 1
        print("hitboxes.json 이 그림과 같다")
        return 0

    OUT.write_text(text, encoding="utf-8")
    for key, value in data.items():
        if not key.startswith("_"):
            print(f"  {key:<26} 사각형 {len(value['rects']):3d}장 · 배율 {value['scale']}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 8: 도구를 돌린다**

Run: `python3 tools/extract_hitboxes.py`
Expected:
```
  martial_hero/attack/4      사각형  18장 · 배율 2.5
  martial_hero/attack2/4     사각형  16장 · 배율 2.5
  medieval_king/attack/2     사각형  13장 · 배율 5.5
  medieval_king/attack2/2    사각형  23장 · 배율 5.5
  medieval_king/attack3/2    사각형  31장 · 배율 5.5
```

Run: `python3 tools/extract_hitboxes.py --check`
Expected: `hitboxes.json 이 그림과 같다` (두 번 돌려도 같은 글자다)

- [ ] **Step 9: 통과하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~HitShapeTableTests`
Expected: `Passed! - Failed: 0, Passed: 7`

- [ ] **Step 10: `.uid` · 게이트 · PNG**

Run: `tools/build.sh import && ls overfit/battle/rules/HitShapeTable.cs.uid`
Expected: 파일이 있다

Run: `tools/build.sh check`
Expected: 0 warning · 0 error · 전부 통과 · `.uid 짝 — 전부 있음`

Run: `git ls-files overfit | grep -c '\.png$'; git status --short | grep -c '\.png'`
Expected: `0` 과 `0` — PNG 가 스테이지에도 없다

- [ ] **Step 11: 커밋** (새 셸 호출)

```bash
git add overfit/battle/rules/HitShapeTable.cs overfit/battle/rules/HitShapeTable.cs.uid \
  overfit/data/hitboxes.json tools/extract_hitboxes.py tools/install_assets.py \
  tests/Overfit.Rules.Tests/Overfit.Rules.Tests.csproj tests/Overfit.Rules.Tests/Battle/HitShapeTableTests.cs
git commit -m "판정 모양을 그림에서 뽑는다 — hitboxes.json 과 그 도구 (#59)" \
  -m "기준점은 .tres 의 region 이다. install_assets.py 가 팩 전체의 불투명 범위로 세로를 잘라 두었고 뷰는 그 region 을 그린다 — 원본 프레임의 아래끝을 바닥으로 잡으면 보스는 33px, 파이터는 195px 어긋난 자리에 판정이 선다. 테스트는 PNG 없이 돌므로, 그림에서 따로 잰 흰 궤적의 끝과 한 칸 안인지로 기준점을 본다.

PNG 판독기는 install_assets.py 의 것을 같이 쓴다(png_rgba). 판독기가 둘이면 발 높이를 재는 쪽과 판정을 뽑는 쪽이 다른 픽셀을 보는 날이 온다. 뽑는 규칙(칸 4px · 흰색 235 · 채움 25%)은 hitboxes.json 의 _source 에 둔다." \
  -m "Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: 판정 창 — 판정은 한 틱이 아니라 창 동안 산다

**골든이 안 움직여야 하는 태스크다.** 옛 패턴은 전부 창 = 한 틱이라, 판정을 대는 자리만 옮겨지고 결과는 한 글자도 안 바뀐다. 움직이면 옮기다 무언가를 바꾼 것이다.

**Files:**
- Modify: `overfit/battle/rules/PatternDef.cs` (`PatternStep.ActiveSeconds`)
- Modify: `overfit/battle/rules/HitBox.cs` (`ActiveSeconds` 칸)
- Modify: `overfit/battle/rules/PatternRunner.cs` (창 길이를 싣는다)
- Modify: `overfit/battle/rules/BattleSim.cs` (`LiveSwing` · `TicksFor` · `ResolveLive` · `Step` · `Land`)
- Modify: `tests/Overfit.Rules.Tests/Battle/TestConfigs.cs` (`SweepId` · `Sweep` · `SweepSim`)
- Test: `tests/Overfit.Rules.Tests/Battle/LiveSwingTests.cs`

**Interfaces:**
- Consumes: 없음 (Task 1·2 와 독립)
- Produces:
  - `PatternStep.ActiveSeconds` (`double`, JSON `active_seconds`, 없으면 0 = 한 틱)
  - `HitBox(..., double ActiveSeconds = 0)` — 기존 칸 뒤에 붙는다
  - `public static int BattleSim.TicksFor(double seconds)` — 0 이하는 1, 아니면 `Math.Round(seconds / Dt, AwayFromZero)` (최소 1)
  - `TestConfigs.SweepId` (`"쓸기"`) · `TestConfigs.Sweep(double maxDistance, double activeSeconds, double endAt = 2.0)` · `TestConfigs.SweepSim(double maxDistance, double activeSeconds, double endAt = 2.0)`

- [ ] **Step 1: 시험 패턴을 TestConfigs 에 더한다**

`tests/Overfit.Rules.Tests/Battle/TestConfigs.cs` — `Tell()` 메서드 바로 아래에:

```csharp
    /// <summary>시험 패턴 <see cref="Sweep"/> 의 id.</summary>
    public const string SweepId = "쓸기";

    /// <summary>
    /// 판정 하나짜리 시험 패턴 — 0.5초에 <paramref name="maxDistance"/> 까지 · 높이 0~5000 을 친다
    /// (이슈 #59 · 판정 창). 대시 창을 넓게(1초) 두는 것은 무적의 길이를 <b>파이터 쪽</b>(0.14초)이
    /// 정하게 하기 위해서다. 패리는 안 된다 — 창을 재는 테스트가 패리 갈래로 새지 않게.
    /// </summary>
    public static PatternDef Sweep(double maxDistance, double activeSeconds, double endAt = 2.0) => new()
    {
        Tell = Tell(),
        Tags = new PatternTags
        {
            DashWindow = 1.0,
            DashDirection = "either",
            Jumpable = false,
            AntiAir = false,
            Parryable = false,
            ParryWindow = 0,
            PunishGreed = false,
            Reach = "far",
            Feint = false,
            MultiHit = 1,
            Tracking = false,
            HasGuardBreak = false,
        },
        Timeline = new List<PatternStep>
        {
            new() { T = 0.0, Kind = "windup" },
            new()
            {
                T = 0.5, Kind = "active", Distance = new[] { 0.0, maxDistance }, Height = new[] { 0.0, 5000.0 },
                Damage = 7, ActiveSeconds = activeSeconds,
            },
            new() { T = endAt, Kind = "end" },
        },
    };

    /// <summary>보스가 서서 <see cref="Sweep"/> 만 휘두르는 판. 보스는 안 움직이고 안 죽는다.</summary>
    public static BattleSim SweepSim(double maxDistance, double activeSeconds, double endAt = 2.0) => new(new BattleSetup
    {
        Arena = Arena(),
        Fighter = Fighter(),
        Boss = Boss(maxHealth: 999_999, moveSpeed: 0, patternGap: 0.2),
        PatternIds = new[] { SweepId },
        Patterns = new Dictionary<string, PatternDef> { [SweepId] = Sweep(maxDistance, activeSeconds, endAt) },
        Seed = 1,
        MaxTicks = 60 * 30,
    });
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`tests/Overfit.Rules.Tests/Battle/LiveSwingTests.cs`:

```csharp
using Overfit.Battle.Rules;
using Shouldly;
using Xunit;

namespace Overfit.Rules.Tests.Battle;

/// <summary>
/// 판정 창 (이슈 #59 · 설계 §3.5) — 판정은 한 틱이 아니라 <b>창</b> 동안 산다. 창 안에서 몸에 닿으면
/// 그 휘두름은 끝나고(한 번만 맞는다), 창이 닫힐 때까지 안 닿으면 관측을 하나 남긴다.
/// </summary>
public class LiveSwingTests
{
    /// <summary>판정이 서기 바로 전 틱까지 민다 — 다음 틱에 판정이 선다.</summary>
    private static void UntilJustBefore(BattleSim sim)
    {
        for (int i = 0; i < 600; i++)
        {
            if (sim.NextActiveIn is { } left && left <= BattleSim.Dt + 1e-9)
            {
                return;
            }

            sim.Tick(default);
        }

        throw new ShouldAssertException("판정이 600틱 안에 안 섰다 — 시험 패턴이 안 돈다");
    }

    [Fact]
    public void 창은_초를_틱으로_반올림한다()
    {
        BattleSim.TicksFor(0).ShouldBe(1, "옛 패턴(창 0)은 한 틱이다");
        BattleSim.TicksFor(0.125).ShouldBe(8, "8fps 한 장 = 7.5틱 — 반올림은 한 방향으로");
        BattleSim.TicksFor(1.0 / 6.0).ShouldBe(10);
    }

    [Fact]
    public void 대시_무적이_창_중간에_풀리면_그_뒤에_맞는다()
    {
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0.5);
        UntilJustBefore(sim);

        sim.Tick(new InputFrame(0, false, true, false, false));   // 판정이 서는 틱에 대시가 선다
        for (int i = 0; i < 40 && sim.Events.Count == 0; i++)
        {
            sim.Tick(default);
        }

        sim.Events.Count.ShouldBe(1, "한 번 휘두르면 관측은 하나다");
        sim.Events[0].Verdict.ShouldBe(HitVerdict.Hit, "무적 8틱이 30틱 창보다 먼저 풀렸는데 안 맞았다");
    }

    [Fact]
    public void 창이_한_틱이면_같은_대시가_피한다()
    {
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0);
        UntilJustBefore(sim);

        sim.Tick(new InputFrame(0, false, true, false, false));

        sim.Events.Count.ShouldBe(1);
        sim.Events[0].Verdict.ShouldBe(HitVerdict.Dodged, "옛 패턴의 한 틱 판정은 그대로 피해져야 한다");
    }

    [Fact]
    public void 한_번_휘두르면_한_번만_맞는다()
    {
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0.5);
        int before = sim.Fighter.Health;
        UntilJustBefore(sim);

        for (int i = 0; i < 40; i++)
        {
            sim.Tick(default);
        }

        sim.Events.Count.ShouldBe(1);
        sim.Fighter.Health.ShouldBe(before - 7, "30틱 동안 서 있었는데 두 번 이상 맞았다");
    }

    [Fact]
    public void 창이_닫힐_때까지_안_닿으면_빗나감_하나만_남긴다()
    {
        // 사거리 50 — 파이터(480)는 보스(1440)와 960 떨어져 한 번도 안 닿는다.
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 50, activeSeconds: 0.5);
        UntilJustBefore(sim);

        for (int i = 0; i < 29; i++)
        {
            sim.Tick(default);
            sim.Events.Count.ShouldBe(0, $"창이 살아 있는데({i + 1}틱째) 관측이 먼저 나왔다");
        }

        sim.Tick(default);   // 30번째 — 창이 닫힌다
        sim.Events.Count.ShouldBe(1);
        sim.Events[0].Verdict.ShouldBe(HitVerdict.MissedTooFar);
    }

    [Fact]
    public void 패턴이_끝나는_틱에_선_판정도_제_이름으로_남는다()
    {
        // end 가 active 와 같은 0.5초다. 러너가 그 틱에 끝나 _current 와 CurrentPattern 이 지워져도,
        // 판정은 낼 때 잡아 둔 태그와 이름으로 대져야 한다.
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0, endAt: 0.5);
        UntilJustBefore(sim);

        sim.Tick(default);

        sim.Events.Count.ShouldBe(1);
        sim.Events[0].PatternId.ShouldBe(TestConfigs.SweepId);
        sim.Events[0].Verdict.ShouldBe(HitVerdict.Hit);
    }
}
```

- [ ] **Step 3: 실패하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~LiveSwingTests`
Expected: 빌드 실패 — `'PatternStep' does not contain a definition for 'ActiveSeconds'` 와 `'BattleSim' does not contain a definition for 'TicksFor'`

- [ ] **Step 4: `PatternStep.ActiveSeconds` 를 더한다**

`overfit/battle/rules/PatternDef.cs` — `PatternStep` 의 `public bool GuardBreak { get; init; }` (와 그 주석) 바로 아래:

```csharp
    /// <summary>
    /// active 가 <b>몇 초 동안</b> 살아 있나 (이슈 #59 · 설계 §3.5). 0 이면 한 틱이다 — 옛 패턴은 전부 그렇다.
    ///
    /// <para>
    /// 판정이 한 틱이면 흰 궤적이 눈앞에 떠 있는데 그 뒤 틱에 걸어 들어간 사람이 안 맞는다. 그림의 궤적
    /// 한 장(8fps = 0.125초) 동안 판정이 살아 있어야 보이는 것이 곧 맞는 것이 된다. 초를 틱으로 바꾸는
    /// 반올림은 <c>BattleSim.TicksFor</c> 한 곳이다.
    /// </para>
    /// </summary>
    public double ActiveSeconds { get; init; }
```

- [ ] **Step 5: `HitBox` 에 창 길이를 싣는다**

`overfit/battle/rules/HitBox.cs` — 레코드의 마지막 칸 `bool Finisher = false);` 를 이렇게 바꾸고, 그 바로 위 `<param name="Finisher">` 주석 블록 뒤에 새 `<param>` 을 더한다:

```csharp
/// <param name="ActiveSeconds">이 판정이 살아 있는 초 (이슈 #59). 0 이면 한 틱이다 —
/// <see cref="PatternStep.ActiveSeconds"/> 를 그대로 싣는다. 틱으로 바꾸는 것은 <c>BattleSim.TicksFor</c> 다.</param>
```

```csharp
    bool Finisher = false,
    double ActiveSeconds = 0);
```

- [ ] **Step 6: 러너가 창 길이를 싣게 한다**

`overfit/battle/rules/PatternRunner.cs` — `hits.Add(new HitBox(` 문을 이렇게 바꾼다:

```csharp
            hits.Add(new HitBox(
                step.Distance[0], step.Distance[1], step.Height[0], step.Height[1], step.Damage,
                step.GuardBreak, Finisher: _next - 1 == _finisher, ActiveSeconds: step.ActiveSeconds));
```

그리고 클래스 주석의 `<para>` 중 "판정은 구간 내내 켜져 있지 않고 <b>단계에 들어선 틱에 한 번</b> 난다" 문단을 이렇게 바꾼다 (낡은 주석은 없느니만 못하다):

```csharp
/// <para>
/// 판정은 <b>단계에 들어선 틱에 한 번</b> 나고, 그 판정이 몇 틱 동안 살아 있을지는 싣기만 한다
/// (<see cref="HitBox.ActiveSeconds"/>) — 살아 있는 동안 몸에 대 보는 것은 <c>BattleSim</c> 이다.
/// 그래서 <c>multi_hit</c> 을 타임라인의 active 개수로 셀 수 있고, 한 번 휘두른 칼에 여러 번 맞는 일이 없다.
/// </para>
```

- [ ] **Step 7: `BattleSim` 이 판정을 창 동안 살려 둔다**

`overfit/battle/rules/BattleSim.cs` 에서 다섯 곳을 고친다.

**(a)** `private readonly List<DodgeEvent> _events = new();` 바로 아래에:

```csharp
    /// <summary>
    /// 살아 있는 보스 판정들 (이슈 #59 · 설계 §3.5). 보통 0~1개다. 러너가 판정을 내는 틱에 들어오고,
    /// 몸에 닿거나 창이 닫히면 나간다.
    /// </summary>
    private readonly List<LiveSwing> _live = new();
```

**(b)** `public BattleOutcome? Tick(InputFrame input)` 안의 `AdvanceBoss();` 바로 뒤에 한 줄:

```csharp
        AdvanceBoss();
        ResolveLive();
        Strike();
```

**(c)** `AdvanceBoss` 안의 판정 루프를 바꾼다. 지금:

```csharp
        foreach (HitBox box in _runner.Tick(Dt))
        {
            Land(box);
        }
```

바꾼 뒤:

```csharp
        foreach (HitBox box in _runner.Tick(Dt))
        {
            // 판정은 여기서 대지 않고 **살려 둔다** (이슈 #59). 창이 몇 틱이든 대는 곳은 ResolveLive 하나다.
            // 태그와 패턴 id 를 지금 잡아 두는 것은 러너가 이 틱 끝에 끝나면 _current 와 CurrentPattern 이
            // 지워지기 때문이다 — 마지막 판정이 end 와 같은 틱에 서면 그 관측이 "?" 패턴으로 남는다.
            _live.Add(new LiveSwing(box, _current!.Tags, Boss.CurrentPattern ?? "?", TicksFor(box.ActiveSeconds)));
        }
```

**(d)** `Land(HitBox box)` 를 **통째로** 아래 넷으로 바꾼다 — `TicksFor` · `ResolveLive` · `Step` · 새 `Land`. 새 `Land` 의 몸통은 옛 `Land` 와 같고, 바뀌는 것은 표시한 넷뿐이다: ① 첫 줄의 `HitVerdict verdict = HitResolver.Resolve(...)` 가 사라지고 판정을 인자로 받는다, ② `box` 는 `swing.Box` 다, ③ `_current.Tags` 는 `swing.Tags` 다, ④ `Boss.CurrentPattern` 은 `swing.PatternId` 다 (관측 레코드와 로그 줄 둘 다).

```csharp
    /// <summary>
    /// 판정 창 길이(초)를 틱으로. <b>반올림은 여기 한 곳이다</b> (설계 §3.5) — 8fps 한 장은 0.125초 =
    /// 7.5틱이라, 뷰와 규칙이 각자 반올림하면 반 틱씩 어긋난다. 0 이하는 한 틱 — 옛 패턴은 전부 그렇다.
    /// </summary>
    public static int TicksFor(double seconds) =>
        seconds <= 0 ? 1 : Math.Max(1, (int)Math.Round(seconds / Dt, MidpointRounding.AwayFromZero));

    /// <summary>살아 있는 판정을 전부 이 틱에 대 본다. 끝난 것은 빼고 산 것은 순서대로 남긴다.</summary>
    private void ResolveLive()
    {
        int kept = 0;
        for (int i = 0; i < _live.Count; i++)
        {
            LiveSwing swing = _live[i];
            if (!Step(swing))
            {
                _live[kept++] = swing;
            }
        }

        _live.RemoveRange(kept, _live.Count - kept);
    }

    /// <summary>
    /// 살아 있는 판정 하나를 이 틱에 대 본다. 끝났으면 true.
    ///
    /// <para>
    /// 몸에 닿는 순간(맞음 · 패리 · 가드 · 붕괴) 그 휘두름은 끝난다 — <b>한 번 휘두르면 한 번만 맞는다.</b>
    /// 무적이 먹은 틱은 넘어가고 창은 계속 산다: 무적이 창보다 먼저 풀리면 그 뒤 틱에 맞는다(다크소울과 같다).
    /// 창이 닫힐 때까지 안 닿았으면 관측을 <b>하나</b> 남긴다 — 어느 틱에 무적이 먹었으면 대시,
    /// 아니면 마지막 틱의 빗나간 이유다.
    /// </para>
    /// </summary>
    private bool Step(LiveSwing swing)
    {
        HitVerdict verdict = HitResolver.Resolve(Fighter, Boss.X, swing.Box, swing.Tags);
        swing.TicksLeft--;

        switch (verdict)
        {
            case HitVerdict.Hit or HitVerdict.Parried or HitVerdict.Guarded or HitVerdict.GuardBroken:
                Land(swing, verdict);
                return true;

            case HitVerdict.Dodged:
                swing.Dodged = true;
                break;

            default:
                swing.LastMiss = verdict;
                break;
        }

        if (swing.TicksLeft > 0)
        {
            return false;
        }

        Land(swing, swing.Dodged ? HitVerdict.Dodged : swing.LastMiss);
        return true;
    }

    /// <summary>판정 하나가 끝났다 — 결과를 몸에 싣고 관측을 남긴다.</summary>
    private void Land(LiveSwing swing, HitVerdict verdict)
    {
        HitBox box = swing.Box;
        switch (verdict)
        {
            // ← 옛 Land 의 switch 그대로 (Hit · Parried · Guarded · GuardBroken · default).
            //   주석도 그대로 둔다. `if (box.Finisher) { Boss.Stagger(); }` 도 그대로다.
        }

        // ← 옛 Land 의 나머지 그대로. 바꾸는 곳은 셋:
        //   PatternId: swing.PatternId,                     (옛: Boss.CurrentPattern ?? "?")
        //   DashAvailable/JumpAvailable/ParryAvailable 의 _current.Tags → swing.Tags
        //   로그 줄의 pattern={Boss.CurrentPattern} → pattern={swing.PatternId}
    }
```

> ⚠ 위 `Land` 의 두 `←` 주석 자리는 **옛 코드를 옮겨 붙이라는 표시**다. 옛 `Land(HitBox box)` 의 `switch` 블록과 그 뒤 몸통(관측 레코드를 만드는 `_events.Add(...)` 와 `Log.Info("dodge", ...)`)을 **한 글자도 빼지 않고** 옮기고, 표시한 세 곳만 바꾼다. 옮긴 뒤 `←` 주석은 지운다.

**(e)** 클래스 맨 아래(`private bool _struckThisSwing;` 뒤)에:

```csharp
    /// <summary>
    /// 살아 있는 판정 하나 (이슈 #59). 러너가 판정을 내는 순간의 태그와 패턴 id 를 <b>들고 다닌다</b> —
    /// 창이 러너보다 오래 살 수 있고, 러너가 끝나면 <see cref="_current"/> 가 지워진다.
    /// </summary>
    private sealed class LiveSwing
    {
        public LiveSwing(HitBox box, PatternTags tags, string patternId, int ticks)
        {
            Box = box;
            Tags = tags;
            PatternId = patternId;
            TicksLeft = ticks;
        }

        public HitBox Box { get; }

        public PatternTags Tags { get; }

        public string PatternId { get; }

        /// <summary>남은 틱. 대 볼 때마다 하나씩 준다.</summary>
        public int TicksLeft { get; set; }

        /// <summary>어느 틱에 무적이 먹었나 — 창이 안 닿고 닫히면 이것이 관측의 답이다.</summary>
        public bool Dodged { get; set; }

        /// <summary>마지막으로 빗나간 이유 — 창이 닫힐 때 무적이 한 번도 안 먹었으면 이것이 답이다.</summary>
        public HitVerdict LastMiss { get; set; } = HitVerdict.MissedTooFar;
    }
```

- [ ] **Step 8: 통과하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~LiveSwingTests`
Expected: `Passed! - Failed: 0, Passed: 6`

- [ ] **Step 9: 골든이 안 움직였는지 본다** — 이 태스크의 진짜 검사다

Run: `tools/build.sh check`
Expected: 0 warning · 0 error · **전부 통과** — `ReplayGoldenTests` 가 옛 골든(`51 1840 0 200 25 e552749d39854b0c`) 그대로 초록이다.

골든이 빨가면 옮기다 무언가를 바꾼 것이다 — 대부분 `Land` 로 옮긴 몸통의 한 줄이 빠졌거나, 관측을 남기는 순서가 달라진 것이다. **골든을 고치지 말고** 원인을 찾는다.

- [ ] **Step 10: 커밋** (새 셸 호출)

```bash
git add overfit/battle/rules/PatternDef.cs overfit/battle/rules/HitBox.cs overfit/battle/rules/PatternRunner.cs \
  overfit/battle/rules/BattleSim.cs tests/Overfit.Rules.Tests/Battle/TestConfigs.cs \
  tests/Overfit.Rules.Tests/Battle/LiveSwingTests.cs
git commit -m "판정을 창 동안 살려 둔다 — 한 번 휘두르면 한 번만 맞는다 (#59)" \
  -m "판정이 한 틱이면 흰 궤적이 눈앞에 떠 있는데 그 뒤 틱에 걸어 들어간 사람이 안 맞는다. 판정은 active_seconds 동안 살고, 몸에 닿는 순간 끝나고, 창이 닫힐 때까지 안 닿으면 관측을 하나 남긴다 — 무적이 창보다 먼저 풀리면 그 뒤 틱에 맞는다(다크소울).

옛 패턴은 전부 창이 한 틱이라 판정을 대는 자리만 옮겨졌고 리플레이 골든이 한 글자도 안 움직였다. 살아 있는 판정은 태그와 패턴 id 를 낼 때 잡아 둔다 — 러너가 그 틱에 끝나면 _current 가 지워지기 때문이다." \
  -m "Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: 몸통 대 모양 — 모든 판정이 모양으로 간다

**골든이 움직이는 태스크다.** 옛 판정은 파이터를 **점**(발 중심)으로 봤다. 이제 몸통(중심 ± 30 · 키 120)이 모양과 겹치는지를 본다 — 띠의 양 끝이 몸 반폭만큼 넓어진 셈이라 경계에 섰던 판정의 결과가 바뀐다. 그리고 다이제스트는 판정 결과를 **이름**으로 싣는데 `MissedTooClose` 가 `MissedByGap` 이 된다.

**Files:**
- Modify: `overfit/battle/rules/HitBox.cs` (띠 네 칸 → `HitShape Shape`)
- Modify: `overfit/battle/rules/PatternRunner.cs` (`ShapeOf`)
- Modify: `overfit/battle/rules/HitResolver.cs` (`Placement` · 몸통 대 모양 · `MissedByGap`)
- Modify: `overfit/battle/rules/Fighter.cs` (`Body`)
- Modify: `overfit/battle/rules/Boss.cs` (`BossConfig.Height` · `Y` · `Body`)
- Modify: `overfit/data/bosses.json` (`height` · `_note_height`)
- Modify: `overfit/battle/rules/BattleSim.cs` (`Step` · 대시 반사실 · `Credit` · `Strike`)
- Modify: `overfit/battle/rules/PlayerAxes.cs` · `overfit/battle/rules/DodgeEvent.cs` (이름)
- Modify: `tools/replay_golden.txt`
- Test: `tests/Overfit.Rules.Tests/Battle/HitResolverTests.cs` · `PatternRunnerTests.cs` · `BattleSimTests.cs` · `PlayerAxesTests.cs` · `TestConfigs.cs`

**Interfaces:**
- Consumes: Task 1 의 `HitRect` · `Placement` · `ShapeContact` · `HitShape` (`Band` · `Reach`) · `ShapeHit` · Task 3 의 `LiveSwing` · `Step`
- Produces:
  - `HitBox(HitShape Shape, int Damage, bool GuardBreak = false, bool Finisher = false, double ActiveSeconds = 0)`
  - `public static HitShape PatternRunner.ShapeOf(PatternStep step)` — **모양을 짓는 곳은 여기 하나다** (Task 5 가 "다음 판정" 을 그릴 때 이것을 부른다)
  - `HitResolver.Resolve(Fighter fighter, Placement at, HitBox box, PatternTags tags)`
  - `HitVerdict.MissedByGap` (옛 `MissedTooClose` 자리 — 값 순서 그대로)
  - `Fighter.Body` · `Boss.Body` (`HitRect`, 월드) · `Boss.Y` (`double`, 지금은 늘 0) · `BossConfig.Height` (`required double`)

- [ ] **Step 1: 실패하는 테스트를 쓴다 — 보는 쪽이 판정에 실리는가**

`tests/Overfit.Rules.Tests/Battle/HitResolverTests.cs` — 클래스 끝(마지막 `}` 앞)에:

```csharp
    [Fact]
    public void 왼쪽을_보는_보스의_모양은_왼쪽을_친다()
    {
        // 앞으로만 치는 모양. 옛 띠는 좌우 대칭이라 "보는 쪽" 이 판정에 실리는지를 한 번도 드러낸 적이 없다.
        var box = new HitBox(new HitShape(new[] { new HitRect(50, 300, 0, 200) }), 18);
        var facingLeft = new Placement(_bossX, 0, -1);

        HitResolver.Resolve(Spawn(_bossX - 150), facingLeft, box, Tags(false)).ShouldBe(HitVerdict.Hit);
        HitResolver.Resolve(Spawn(_bossX + 150), facingLeft, box, Tags(false))
            .ShouldBe(HitVerdict.MissedTooFar, "등 뒤를 쳤다 — 보는 쪽이 판정에 안 실렸다");
    }

    [Fact]
    public void 몸통이_닿으면_중심이_밖이어도_맞는다()
    {
        // 띠 [0, 260] · 파이터 중심 280 — 옛 판정(몸을 점으로 봤다)은 빗나감이었다. 몸 왼끝이 250 이라 닿는다.
        HitResolver.Resolve(Spawn(_bossX + 280), _at, Mid(), Tags(false)).ShouldBe(HitVerdict.Hit);
    }
```

그리고 `private static HitBox Mid()` 위에 한 줄:

```csharp
    /// <summary>보스 발밑에서 오른쪽을 본다 — 파이터는 보스 오른쪽에 선다(Spawn(_bossX + …)).</summary>
    private static readonly Placement _at = new(_bossX, 0, 1);
```

Run: `tools/build.sh test --filter FullyQualifiedName~HitResolverTests`
Expected: 빌드 실패 — `HitBox` 생성자에 `HitShape` 를 넣을 수 없다 · `Resolve` 가 `Placement` 를 안 받는다

- [ ] **Step 2: `HitBox` 가 띠 대신 모양을 든다**

`overfit/battle/rules/HitBox.cs` — 레코드와 그 주석을 이렇게 바꾼다. `GuardBreak` · `Finisher` · `ActiveSeconds` 의 `<param>` 주석은 **지금 것 그대로** 둔다; 바뀌는 것은 요약 주석과 앞의 칸들이다:

```csharp
/// <summary>
/// 판정 하나 — <b>모양</b>과 피해 (이슈 #59 · 설계 §3). 모양은 공격자 기준이고, 어디에 놓을지(보스의 발 ·
/// 보는 쪽)는 판정할 때 <see cref="Placement"/> 로 준다. 옛 판정은 "보스 중심에서 거리 몇~몇 · 높이 몇~몇" 인
/// 좌우 대칭 띠였고, 그 띠는 이제 <see cref="HitShape.Band"/> 두 장으로 같은 통로를 탄다.
/// </summary>
/// <param name="Shape">판정 모양 (공격자 기준 사각형들).</param>
/// <param name="Damage">막지 않았을 때의 피해.</param>
```

```csharp
public readonly record struct HitBox(
    HitShape Shape,
    int Damage,
    bool GuardBreak = false,
    bool Finisher = false,
    double ActiveSeconds = 0);
```

- [ ] **Step 3: 러너가 모양을 짓는다 — 한 곳에서**

`overfit/battle/rules/PatternRunner.cs` — `hits.Add(new HitBox(` 문을:

```csharp
            hits.Add(new HitBox(
                ShapeOf(step), step.Damage, step.GuardBreak, Finisher: _next - 1 == _finisher,
                ActiveSeconds: step.ActiveSeconds));
```

그리고 클래스 안(`Tick` 메서드 뒤)에:

```csharp
    /// <summary>
    /// 타임라인 한 단계의 판정 모양. <b>모양을 짓는 곳은 여기 하나다</b> — 러너가 판정을 낼 때와
    /// 디버그 표시가 "다음 판정" 을 그릴 때가 같은 함수를 불러야 둘이 같은 자리를 가리킨다.
    /// 지금은 옛 거리 띠 · 높이 띠를 좌우 대칭 두 장으로 옮긴다 (<see cref="HitShape.Band"/>).
    /// </summary>
    public static HitShape ShapeOf(PatternStep step)
    {
        ArgumentNullException.ThrowIfNull(step);
        if (step.Distance is null || step.Height is null)
        {
            throw new ArgumentException($"판정이 아닌 단계다 — kind={step.Kind}", nameof(step));
        }

        return HitShape.Band(step.Distance[0], step.Distance[1], step.Height[0], step.Height[1]);
    }
```

- [ ] **Step 4: 몸통을 둔다 — 파이터 · 보스**

`overfit/battle/rules/Fighter.cs` — `public double HalfWidth => _config.HalfWidth;` 바로 아래:

```csharp
    /// <summary>
    /// 몸통 — 발 중심에서 좌우 반폭, 발바닥에서 키만큼 (월드). <b>판정은 이것과 모양을 겹쳐 본다</b>
    /// (이슈 #59 · 설계 §3.4). 옛 판정은 몸을 점(발 중심)으로 봤다.
    /// </summary>
    public HitRect Body => new(X - _config.HalfWidth, X + _config.HalfWidth, Y, Y + _config.Height);
```

`overfit/battle/rules/Boss.cs` — `BossConfig` 의 `public required double HalfWidth { get; init; }` 바로 아래:

```csharp
    /// <summary>
    /// 몸 키(px). 반폭과 같이 <b>그려지는 몸</b>에서 잰다 — bosses.json 의 <c>_note_height</c> 에 어떻게 쟀는지 있다.
    /// 파이터의 칼이 이것에 대 본다 (이슈 #59).
    /// </summary>
    public required double Height { get; init; }
```

`Boss` 클래스 — `public double X { get; private set; }` 바로 아래:

```csharp
    /// <summary>
    /// 발바닥 높이 (바닥 0). <b>지금은 늘 0 이다</b> — 뛰어오르는 패턴(설계 §4.2)이 들어올 때 움직인다.
    /// 판정은 이것을 모양을 놓는 자리로 쓴다(<see cref="Placement"/>).
    /// </summary>
    public double Y { get; }

    /// <summary>몸통 — 중심 ± 반폭, 발바닥에서 키만큼 (월드). 파이터의 칼이 이것에 대 본다 (이슈 #59).</summary>
    public HitRect Body => new(X - _config.HalfWidth, X + _config.HalfWidth, Y, Y + _config.Height);
```

`overfit/data/bosses.json` — `"_note_half_width"` 줄 바로 아래에 새 메모, 그리고 `"그림"` 의 `"half_width": 85,` 뒤에 `"height": 297,`:

```json
  "_note_height": "몸 키도 **그려지는 몸**에서 잰다 (이슈 #59). idle 첫 장을 .tres 의 region(2..105) 안에서 재면 불투명 세로가 49..102 라 54행 × 5.5배 = 297.0 이고, 같은 장의 폭 170.5 가 half_width 85 와 맞는다. 파이터의 칼(판정 모양)이 이 몸통에 대 보므로, 팩을 갈면 반폭과 같이 옮긴다.",
```

```json
    "half_width": 85, "height": 297, "pattern_gap": 0.8,
```

`tests/Overfit.Rules.Tests/Battle/TestConfigs.cs` — `Boss(...)` 의 `HalfWidth = data.HalfWidth,` 바로 아래:

```csharp
            Height = data.Height,
```

- [ ] **Step 5: 판정기가 몸통을 모양에 댄다**

`overfit/battle/rules/HitResolver.cs`:

**(a)** 열거형 — `MissedTooFar` 의 주석과 `MissedTooClose` 칸을 이렇게 바꾼다 (값의 **자리는 그대로** 둔다):

```csharp
    /// <summary>
    /// 몸이 판정 모양의 <b>가로 범위 밖</b>이라 안 닿았다 — 멀어서 피한 것이다(그 거리를 대시가 만들었을 수도 있다).
    ///
    /// <para>
    /// 모양 <b>안쪽의 빈 곳</b>(<see cref="MissedByGap"/>)과 <b>따로</b> 둔다 (이슈 #46 · #59). 한 갈래였을 때는
    /// 파고들어 피한 것과 도망쳐 피한 것이 계측에서 같은 한 점이었다 — 그 둘은 성향이 정반대고
    /// 봉인할 것도 정반대라, 뭉개면 정반대 변종이 뽑힌다.
    /// </para>
    /// </summary>
    MissedTooFar,

    /// <summary>
    /// 판정 모양의 외곽 상자 <b>안인데 빈 칸</b>이라 안 닿았다 — 초승달 안쪽 같은 곳 (이슈 #59 · 설계 §7.1).
    /// 옛 "안쪽 주머니"(<c>MissedTooClose</c>, 이슈 #46)가 이것의 한 경우다: 좌우 대칭 띠 두 장 사이의 빈 곳이다.
    /// </summary>
    MissedByGap,
```

**(b)** `Resolve` — 시그니처와 앞부분(거리 · 높이 검사)을 모양 검사로 바꾼다. **대시 · 패리 · 가드 · 맞음 갈래와 그 주석은 그대로 둔다.** 지금:

```csharp
    public static HitVerdict Resolve(Fighter fighter, double bossX, HitBox box, PatternTags tags)
    {
        ArgumentNullException.ThrowIfNull(fighter);
        ArgumentNullException.ThrowIfNull(tags);

        // (거리를 재고 MissedTooFar / MissedTooClose 를 돌려주는 블록)
        // (몸통 높이를 재고 MissedByHeight 를 돌려주는 블록)
```

바꾼 뒤 — 두 블록(과 그 주석)을 통째로 이것으로:

```csharp
    public static HitVerdict Resolve(Fighter fighter, Placement at, HitBox box, PatternTags tags)
    {
        ArgumentNullException.ThrowIfNull(fighter);
        ArgumentNullException.ThrowIfNull(tags);

        // 몸통을 모양에 댄다 (이슈 #59 · 설계 §3.5). 안 닿으면 왜 안 닿았는지를 그대로 싣는다 —
        // 멀어서 · 넘어서 · 틈에 서서는 플레이어가 한 일이 서로 다르고, 그 셋을 가르는 것이 계측이다.
        switch (ShapeHit.Test(box.Shape, at, fighter.Body))
        {
            case ShapeContact.TooFar:
                return HitVerdict.MissedTooFar;
            case ShapeContact.ByHeight:
                return HitVerdict.MissedByHeight;
            case ShapeContact.ByGap:
                return HitVerdict.MissedByGap;
            default:
                break;
        }
```

- [ ] **Step 6: 이름을 바꾼다 — `MissedTooClose` → `MissedByGap`**

```bash
grep -rl 'MissedTooClose' overfit tests | xargs sed -i '' 's/MissedTooClose/MissedByGap/g'
grep -rn 'MissedTooClose' overfit tests || echo "(남은 것 없음)"
```
Expected: `(남은 것 없음)`

그 뒤 **주석을 읽고 고친다** — 이름만 바뀌고 뜻이 낡은 자리가 있다:
- `overfit/battle/rules/PlayerAxes.cs` 의 `DistanceBias` 주석: "`MissedByGap` 은 모양 안쪽 빈 곳에서 빗나간 것이라 …" 로 — 옛 "너무 가까워서" 는 이제 "모양의 빈 곳에서" 다. 부호를 뒤집는 규칙(이슈 #46)은 그대로다.
- `overfit/battle/rules/DodgeEvent.cs` 의 `Spacing` 주석: "안(`MissedByGap`)과 밖(`MissedTooFar`)" 으로 이미 바뀌었는지 본다.

- [ ] **Step 7: `BattleSim` 이 모양으로 판정한다**

`overfit/battle/rules/BattleSim.cs`:

**(a)** `Step` 의 첫 줄:

```csharp
        HitVerdict verdict = HitResolver.Resolve(Fighter, new Placement(Boss.X, Boss.Y, Boss.Facing), swing.Box, swing.Tags);
```

**(b)** 대시 반사실을 모양으로 바꾼다. 필드 `private double _dashStartDistance = double.NaN;` (와 그 주석 블록)을 이것으로:

```csharp
    /// <summary>
    /// 대시를 <b>시작하기 직전</b>의 몸통과 그때의 보스 자리 (null = 대시 중이 아니다).
    /// "그 자리에 서 있었으면 이 판정에 맞았나" 를 판정마다 물어보는 반사실(counterfactual)이다 —
    /// 맞았을 것이면 대시가 빼낸 것이고, 거기서도 안 맞았으면 간격이다 (이슈 #46).
    ///
    /// <para>
    /// 대시 중이라는 것만으로는 부족하다. 사거리 100 짜리 판정 앞에서 960px 떨어져 대시하면
    /// 대시는 돌지만 그 거리는 대시가 만든 것이 아니다 — 그것까지 대시의 공으로 돌리면
    /// <c>dash_timing_bias</c> 가 "판정을 피한 대시" 가 아닌 것들로 채워진다.
    /// 옛 반사실은 거리 하나(보스 중심에서)를 띠와 견줬고, 이제는 몸통을 모양에 댄다 (이슈 #59).
    /// </para>
    /// </summary>
    private HitRect? _dashStartBody;

    private double _dashStartBossX;
```

`Tick` 안 — `double wasX = Fighter.X;` 바로 아래:

```csharp
        double wasY = Fighter.Y;
```

그리고 `RememberDodgeStart(input, wasGrounded, wasX);` 를 `RememberDodgeStart(input, wasGrounded, wasX, wasY);` 로.

`RememberDodgeStart` — 시그니처에 `double wasY` 를 더하고(`<param name="wasY">이번 틱이 시작될 때의 발바닥 높이. 공중 대시의 반사실이 이것을 쓴다.</param>` 주석도), 대시 갈래 둘을 이렇게:

```csharp
        if (Fighter.Action == FighterAction.Dash && Fighter.ActionElapsed <= Dt)
        {
            _dashStartedAt = now;
            // 보스 쪽으로 갔으면 안(+1), 반대면 밖(-1)
            _dashDirection = Math.Sign(Fighter.Facing * (Boss.X - Fighter.X)) >= 0 ? 1 : -1;
            // 보스는 아직 이번 틱을 안 밀었으므로(AdvanceBoss 는 뒤에 온다) 둘 다 틱 시작의 자리다.
            _dashStartBody = new HitRect(
                wasX - Fighter.HalfWidth, wasX + Fighter.HalfWidth, wasY, wasY + Fighter.BodyHeight);
            _dashStartBossX = Boss.X;
        }
        else if (Fighter.Action != FighterAction.Dash)
        {
            _dashStartedAt = double.NaN;
            _dashDirection = 0;
            _dashStartBody = null;
        }
```

`CreditDistance` 의 식을 이렇게 (위의 긴 주석은 그대로 둔다):

```csharp
    private (DodgeVerb Verb, double StartedAt) CreditDistance(HitBox box) =>
        !double.IsNaN(_dashStartedAt)
        && _dashStartBody is { } before
        && ShapeHit.Test(box.Shape, new Placement(_dashStartBossX, Boss.Y, Boss.Facing), before) == ShapeContact.Overlap
            ? (DodgeVerb.Dash, _dashStartedAt)
            : (DodgeVerb.Spacing, double.NaN);
```

**(c)** `Credit` 의 거리 갈래 — Step 6 의 이름 바꾸기가 이미 `HitVerdict.MissedTooFar or HitVerdict.MissedByGap => CreditDistance(box),` 로 만들어 두었는지 본다.

**(d)** 파이터의 칼을 모양으로. 필드 `private readonly BattleSetup _setup;` 바로 아래:

```csharp
    /// <summary>
    /// 파이터 칼의 판정 모양 (이슈 #59). 지금은 옛 사거리를 그대로 옮긴 것이다(<see cref="HitShape.Reach"/>) —
    /// 옛 판정 <c>|dx| − 보스 반폭 ≤ 사거리</c> 와 가로가 정확히 같다. 그림의 모양으로 가는 것은 설계 §10 의 2번이다.
    /// </summary>
    private readonly HitShape _attackShape;
```

생성자 — `_setup = setup;` 바로 아래:

```csharp
        _attackShape = HitShape.Reach(setup.Fighter.AttackReach);
```

`Strike` — 지금:

```csharp
        double gap = Math.Abs(Fighter.X - Boss.X) - Boss.HalfWidth;
        if (gap <= Fighter.AttackReach)
        {
```

바꾼 뒤:

```csharp
        double gap = Math.Abs(Fighter.X - Boss.X) - Boss.HalfWidth;
        if (ShapeHit.Test(_attackShape, new Placement(Fighter.X, Fighter.Y, Fighter.Facing), Boss.Body)
            == ShapeContact.Overlap)
        {
```

(`gap` 은 로그 줄이 계속 쓰므로 남긴다.)

- [ ] **Step 8: 옛 시그니처를 쓰던 테스트를 옮긴다**

`tests/Overfit.Rules.Tests/Battle/HitResolverTests.cs`:

```bash
sed -i '' 's/HitResolver.Resolve(\(.*\), _bossX, /HitResolver.Resolve(\1, _at, /' tests/Overfit.Rules.Tests/Battle/HitResolverTests.cs
grep -c 'HitResolver.Resolve(.*, _at, ' tests/Overfit.Rules.Tests/Battle/HitResolverTests.cs
grep -n 'HitResolver.Resolve(' tests/Overfit.Rules.Tests/Battle/HitResolverTests.cs | grep -v '_at, \|facingLeft, ' || echo "(전부 옮겼다)"
```
Expected: 마지막 줄이 `(전부 옮겼다)`

같은 파일의 띠 도우미 여섯 곳 — 다섯 숫자 `(min, max, low, high, damage, …)` 를 `(HitShape.Band(min, max, low, high), damage, …)` 로:

```csharp
    private static HitBox Mid() => new(HitShape.Band(0, 260, 0, 200), 18);
    private static HitBox Low() => new(HitShape.Band(0, 520, 0, 70), 14);
    private static HitBox High() => new(HitShape.Band(0, 300, 140, 420), 20);
    private static HitBox Pocket() => new(HitShape.Band(190, 760, 0, 330), 14);
    private static HitBox Unguardable() => new(HitShape.Band(0, 260, 0, 200), 18, GuardBreak: true);
```
그리고 `var box = new HitBox(0, 10, 0, 200, 18);` → `var box = new HitBox(HitShape.Band(0, 10, 0, 200), 18);`

`tests/Overfit.Rules.Tests/Battle/PatternRunnerTests.cs` — 띠 네 칸을 읽던 단언 넷을 모양 단언으로:

```csharp
        box.Shape.Local.ShouldBe(new[] { new HitRect(0, 260, 0, 200), new HitRect(-260, 0, 0, 200) });
        box.Damage.ShouldBe(18);
```

- [ ] **Step 9: 전체를 돌리고, 몸통 때문에만 움직인 셋업을 옮긴다**

Run: `tools/build.sh test`
Expected: `ReplayGoldenTests` 는 **실패한다** (다음 스텝에서 고친다). 그 밖의 실패를 하나씩 본다.

그 밖의 실패마다 이 규칙 하나로 판단한다:

> **파이터가 판정 가장자리에서 30px(몸 반폭) 안에 서 있었나?** 그렇다면 그 테스트는 "몸을 점으로 보던" 경계를 재고 있었다. 파이터를 **정확히 30px** 옮겨 몸의 가장자리가 옛 중심이 있던 자리에 오게 하고(빗나가야 하면 바깥으로, 맞아야 하면 안으로), **단언은 한 글자도 안 고친다.** 그 테스트 주석에 한 줄 남긴다: `// 이슈 #59 — 판정이 몸통 대 모양이 되어 경계가 몸 반폭(30)만큼 옮겨졌다.`
>
> **아니라면** 진짜 회귀다. 고치지 말고 **멈추고 보고한다.**

몇 개를 옮겼는지 적어 둔다 — PR 에 쓴다.

- [ ] **Step 10: 리플레이 골든을 다시 박는다**

Run: `tools/build.sh test --filter FullyQualifiedName~ReplayGolden 2>&1 | grep -A2 '실측'`
Expected: `실측: 51 … …` 한 줄 — 이것이 **새 골든**이다.

`tools/replay_golden.txt` — 맨 끝의 골든 줄 **바로 위**에 이 주석 블록을 더하고(옛 줄 · 새 줄을 채운다), 골든 줄을 새 값으로 바꾼다:

```
# 2026-09-24 갱신 (이슈 #59 · 판정이 몸통 대 모양이 되다): 51 1840 0 200 25 e552749d39854b0c → <새 줄>.
# 옛 판정은 파이터를 **점**(발 중심)으로 보고 보스 중심에서의 거리를 띠와 견줬다. 이제 몸통(중심 ± 반폭 30 ·
# 키 120)이 판정 모양(옛 띠는 좌우 대칭 두 장)과 겹치는지를 본다 — 띠의 양 끝이 몸 반폭만큼 넓어진 셈이라
# 경계에 서 있던 판정의 결과가 바뀐다. 안쪽 주머니는 MissedTooClose 에서 MissedByGap 으로 이름이 바뀌었고
# 다이제스트가 그 이름을 싣는다. 직전 커밋(판정 창)은 창이 한 틱이라 이 줄을 한 글자도 안 움직였다 —
# 움직인 것은 전부 이 커밋의 두 가지다.
```

Run: `tools/build.sh check`
Expected: 0 warning · 0 error · **전부 통과** · `.uid 짝 — 전부 있음`

- [ ] **Step 11: 데모 로그를 줄 단위로 본다** — 이 저장소에서 오기록을 실제로 두 번 잡은 방법이다

Run: `tools/build.sh demo && grep -h '\[dodge\]' out/demo.log | head -40`

모든 `[dodge]` 줄에 대해: `verdict=MissedTooFar` 면 `dist` 가 그 패턴 판정의 바깥끝 + 30 보다 크다. `verdict=MissedByGap` 면 `dist` 가 안쪽끝 − 30 보다 작다. `verdict=Hit` 인데 `dist` 가 바깥끝 + 30 보다 크면 **버그다** — 멈추고 보고한다.

- [ ] **Step 12: 커밋** (새 셸 호출)

```bash
git add -A overfit/battle/rules overfit/data/bosses.json tools/replay_golden.txt tests/Overfit.Rules.Tests
git status --short
git commit -m "모든 판정이 몸통 대 모양이 된다 — 옛 띠는 좌우 대칭 두 장으로 (#59)" \
  -m "옛 판정은 파이터를 점(발 중심)으로 보고 보스 중심에서의 거리를 띠와 견줬다. 그림의 칼은 앞으로 휘두르고 어떤 궤적은 등 뒤까지 감으므로 판정은 모양이어야 하고, 모양은 몸통에 대 봐야 한다. 옛 거리 띠는 좌우 대칭 두 장(Band), 옛 공격 사거리는 높이를 안 보는 한 장(Reach — 옛 부등식과 가로가 정확히 같다)으로 옮겨 같은 통로를 탄다.

띠의 양 끝이 몸 반폭(30)만큼 넓어진 셈이라 경계에 섰던 판정이 바뀌고 리플레이 골든이 움직였다. 경계를 재던 테스트 <N>개는 파이터를 정확히 30px 옮겼고 단언은 안 고쳤다. MissedTooClose 는 MissedByGap 이 됐다 — 옛 안쪽 주머니는 '모양의 빈 곳' 의 한 경우다. 보스 키 297 은 idle 을 region 안에서 잰 값이다." \
  -m "Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

(`<N>` 은 Step 9 에서 적어 둔 수로 채운다.)

---

### Task 5: 판정 기록 — 규칙이 이 틱에 댄 자리를 내놓는다

**Files:**
- Modify: `overfit/battle/rules/BattleSim.cs` (`_tested` · `_attackTested` · 세 조회)
- Test: `tests/Overfit.Rules.Tests/Battle/HitDebugTests.cs`

**Interfaces:**
- Consumes: Task 1 의 `HitShape.Place` · Task 3 의 `ResolveLive` · Task 4 의 `PatternRunner.ShapeOf` · `Boss.Y` · `_attackShape`
- Produces:
  - `IReadOnlyList<HitRect> BattleSim.BossTestedRects` — **이 틱에 규칙이 파이터에게 대 본** 보스 판정 사각형 (월드)
  - `IReadOnlyList<HitRect> BattleSim.BossNextRects` — 선딜 중 **다음** 판정이 칠 자리 (월드). 더 올 판정이 없으면 빈 목록
  - `IReadOnlyList<HitRect> BattleSim.FighterTestedRects` — 이 틱에 규칙이 보스에게 대 본 파이터 칼 (월드)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/Overfit.Rules.Tests/Battle/HitDebugTests.cs`:

```csharp
using System.Linq;
using Overfit.Battle.Rules;
using Shouldly;
using Xunit;

namespace Overfit.Rules.Tests.Battle;

/// <summary>
/// 판정 기록 (이슈 #59 · 설계 §6.1). 디버그 표시는 규칙이 <b>이 틱에 실제로 대 본</b> 사각형을 받아 그리기만 한다 —
/// 여기서 보는 것은 그 조회가 규칙이 댄 자리와 같은가다.
/// </summary>
public class HitDebugTests
{
    private static void UntilJustBefore(BattleSim sim)
    {
        for (int i = 0; i < 600; i++)
        {
            if (sim.NextActiveIn is { } left && left <= BattleSim.Dt + 1e-9)
            {
                return;
            }

            sim.Tick(default);
        }

        throw new ShouldAssertException("판정이 600틱 안에 안 섰다");
    }

    private static Placement BossAt(BattleSim sim) => new(sim.Boss.X, sim.Boss.Y, sim.Boss.Facing);

    [Fact]
    public void 쉬는_동안에는_보스_판정이_안_보인다()
    {
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0.5);
        sim.Tick(default);   // 패턴 간격(0.2초) 안 — 아직 아무 패턴도 안 섰다

        sim.BossTestedRects.ShouldBeEmpty();
        sim.BossNextRects.ShouldBeEmpty();
    }

    [Fact]
    public void 선딜_동안에는_다음_판정이_칠_자리가_보인다()
    {
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0.5);
        UntilJustBefore(sim);

        sim.BossTestedRects.ShouldBeEmpty("아직 판정이 안 섰는데 댄 자리가 있다");
        sim.BossNextRects.ShouldBe(HitShape.Band(0, 5000, 0, 5000).Place(BossAt(sim)));
    }

    [Fact]
    public void 판정이_선_틱에는_규칙이_댄_자리가_보이고_끝나면_사라진다()
    {
        // 파이터는 사거리 5000 안이라 판정이 서는 틱에 맞고 휘두름이 끝난다.
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0.5);
        UntilJustBefore(sim);

        sim.Tick(default);
        sim.BossTestedRects.ShouldBe(HitShape.Band(0, 5000, 0, 5000).Place(BossAt(sim)));
        sim.Events.Count.ShouldBe(1, "이 틱에 맞았어야 한다");

        sim.Tick(default);
        sim.BossTestedRects.ShouldBeEmpty("맞고 끝난 휘두름이 다음 틱에도 그려진다");
    }

    [Fact]
    public void 창이_사는_동안에는_매_틱_보인다()
    {
        // 사거리 50 — 안 닿으므로 30틱 창 내내 대 본다.
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 50, activeSeconds: 0.5);
        UntilJustBefore(sim);

        for (int i = 0; i < 30; i++)
        {
            sim.Tick(default);
            sim.BossTestedRects.Count.ShouldBe(2, $"창 {i + 1}틱째인데 댄 자리가 안 보인다");
        }

        sim.Tick(default);
        sim.BossTestedRects.ShouldBeEmpty("창이 닫혔는데 그려진다");
    }

    [Fact]
    public void 파이터_칼은_규칙이_댄_틱에만_보인다()
    {
        BattleSim sim = TestConfigs.SweepSim(maxDistance: 5000, activeSeconds: 0.5);
        sim.Tick(new InputFrame(0, false, false, false, true));

        int shown = 0;
        for (int i = 0; i < 30; i++)
        {
            if (sim.FighterTestedRects.Count > 0)
            {
                shown++;
                var at = new Placement(sim.Fighter.X, sim.Fighter.Y, sim.Fighter.Facing);
                sim.FighterTestedRects.ShouldBe(HitShape.Reach(TestConfigs.Fighter().AttackReach).Place(at));
            }

            sim.Tick(default);
        }

        shown.ShouldBe(1, "옛 칼 판정은 한 번 휘두를 때 한 틱만 댄다 — 그 틱에만 보여야 한다");
    }
}
```

Run: `tools/build.sh test --filter FullyQualifiedName~HitDebugTests`
Expected: 빌드 실패 — `'BattleSim' does not contain a definition for 'BossTestedRects'`

- [ ] **Step 2: 규칙이 댄 자리를 기록만 해 둔다 — 할당 없이**

`overfit/battle/rules/BattleSim.cs`:

**(a)** `private readonly List<LiveSwing> _live = new();` 바로 아래:

```csharp
    /// <summary>
    /// 이 틱에 파이터에게 대 본 보스 판정 — (모양, 놓은 자리). <b>기록만 한다</b> (이슈 #59 · 설계 §6.1).
    /// 사각형으로 펴는 것은 디버그 표시가 물을 때(<see cref="BossTestedRects"/>)다: 봇이 수백만 판을 돌리는
    /// 동안 이 목록은 용량을 다시 쓸 뿐 새로 할당하지 않는다.
    /// </summary>
    private readonly List<(HitShape Shape, Placement At)> _tested = new();

    /// <summary>이 틱에 보스에게 대 본 파이터 칼의 자리. 안 댔으면 null.</summary>
    private Placement? _attackTested;
```

**(b)** `ResolveLive` — 첫 줄에 `_tested.Clear();`, 그리고 놓는 자리를 한 번 계산해 `Step` 과 기록이 **같은 값**을 쓰게 한다:

```csharp
    private void ResolveLive()
    {
        _tested.Clear();
        var at = new Placement(Boss.X, Boss.Y, Boss.Facing);

        int kept = 0;
        for (int i = 0; i < _live.Count; i++)
        {
            LiveSwing swing = _live[i];
            _tested.Add((swing.Box.Shape, at));
            if (!Step(swing, at))
            {
                _live[kept++] = swing;
            }
        }

        _live.RemoveRange(kept, _live.Count - kept);
    }
```

`Step` 의 시그니처를 `private bool Step(LiveSwing swing, Placement at)` 로, 첫 줄을:

```csharp
        HitVerdict verdict = HitResolver.Resolve(Fighter, at, swing.Box, swing.Tags);
```

**(c)** `Strike` — 메서드 첫 줄에 `_attackTested = null;`, 그리고 모양 검사를 하는 자리에서 놓는 자리를 기록한다:

```csharp
    private void Strike()
    {
        _attackTested = null;
        if (!Fighter.AttackActive || _struckThisSwing)
        {
            if (!Fighter.AttackActive)
            {
                _struckThisSwing = false;
            }

            return;
        }

        double gap = Math.Abs(Fighter.X - Boss.X) - Boss.HalfWidth;
        var at = new Placement(Fighter.X, Fighter.Y, Fighter.Facing);
        _attackTested = at;
        if (ShapeHit.Test(_attackShape, at, Boss.Body) == ShapeContact.Overlap)
        {
            // ← 옛 몸통 그대로 (피해 · 로그)
        }

        _struckThisSwing = true;
    }
```

(`←` 자리는 지금 `if` 안에 있는 피해 · 로그 코드를 그대로 둔다는 표시다. 주석은 지운다.)

**(d)** 세 조회 — `NextActiveGuardBreak` 속성 바로 아래:

```csharp
    /// <summary>
    /// 이 틱에 규칙이 파이터에게 <b>대 본</b> 보스 판정 사각형 (월드) — 디버그 표시용 (이슈 #59 · 설계 §6.1).
    /// 표시가 이것을 받아 그리기만 하므로, 판정이 틀린 자리에 서면 화면도 그 틀린 자리를 보여 준다.
    /// </summary>
    public IReadOnlyList<HitRect> BossTestedRects
    {
        get
        {
            var rects = new List<HitRect>();
            foreach ((HitShape shape, Placement at) in _tested)
            {
                rects.AddRange(shape.Place(at));
            }

            return rects;
        }
    }

    /// <summary>
    /// 선딜 중이면 <b>다음</b> 판정이 칠 자리 (월드) — 어디로 올지 미리 보인다. 러너가 그 판정을 낼 때와
    /// 같은 함수(<see cref="PatternRunner.ShapeOf"/>)로 짓는다. 더 올 판정이 없으면 빈 목록.
    /// </summary>
    public IReadOnlyList<HitRect> BossNextRects =>
        NextActive() is { } step
            ? PatternRunner.ShapeOf(step).Place(new Placement(Boss.X, Boss.Y, Boss.Facing))
            : Array.Empty<HitRect>();

    /// <summary>이 틱에 규칙이 보스에게 <b>대 본</b> 파이터 칼 (월드). 안 댔으면 빈 목록.</summary>
    public IReadOnlyList<HitRect> FighterTestedRects =>
        _attackTested is { } at ? _attackShape.Place(at) : Array.Empty<HitRect>();
```

- [ ] **Step 3: 통과하는지 본다**

Run: `tools/build.sh test --filter FullyQualifiedName~HitDebugTests`
Expected: `Passed! - Failed: 0, Passed: 5`

Run: `tools/build.sh check`
Expected: 0 warning · 0 error · 전부 통과 — **골든이 Task 4 의 값 그대로다** (기록은 판을 안 바꾼다)

- [ ] **Step 4: 커밋** (새 셸 호출)

```bash
git add overfit/battle/rules/BattleSim.cs tests/Overfit.Rules.Tests/Battle/HitDebugTests.cs
git commit -m "규칙이 이 틱에 댄 판정을 내놓는다 — 표시는 그리기만 한다 (#59)" \
  -m "디버그 표시가 hitboxes.json 을 따로 읽어 좌우를 뒤집고 발 위치를 더하면, 규칙 쪽 그 계산에 버그가 있어도 표시는 멀쩡해 보인다. 그래서 규칙이 이 틱에 댄 (모양, 놓은 자리) 를 기록해 두고 표시가 물을 때 사각형으로 편다 — 판정과 표시가 같은 ToWorld 를 탄다.

기록은 할당을 안 한다: 목록을 비우고 다시 채울 뿐이라 봇이 수백만 판을 돌려도 비용이 없다. 선딜 중 다음 판정은 러너가 판정을 낼 때와 같은 ShapeOf 로 짓는다." \
  -m "Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 판정 보기 — Godot 의 Visible Collision Shapes 에 넘긴다

뷰 코드는 규칙 테스트 범위 밖이다 — 검사는 실행해서 본다.

**Files:**
- Create: `overfit/battle/view/HitboxDebug.cs`
- Modify: `overfit/battle/Battle.cs` (세우기 · 매 프레임 넘기기)
- Modify: `tools/build.sh` (`HITBOXES=1`)

**Interfaces:**
- Consumes: Task 4 의 `Fighter.Body` · `Boss.Body` · Task 5 의 `BossTestedRects` · `BossNextRects` · `FighterTestedRects` · 기존 `Fighter.Invulnerable` · `Fighter.Parrying` · `Fighter.Guarding`
- Produces: `HitboxDebug.Show(...)` · `HitboxDebug.FighterColor(bool invulnerable, bool parrying, bool guarding)` · `HITBOXES=1` 환경 변수

- [ ] **Step 1: 뷰를 쓴다**

`overfit/battle/view/HitboxDebug.cs`:

```csharp
using System.Collections.Generic;
using Godot;
using Overfit.Battle.Rules;
using Overfit.Core;

namespace Overfit.Battle.View;

/// <summary>
/// 규칙의 판정 사각형을 <see cref="CollisionShape2D"/> 로 옮겨, Godot 의 <b>Visible Collision Shapes</b>
/// (에디터 Debug 메뉴 · 실행 인자 <c>--debug-collisions</c>)가 그리게 한다 (이슈 #59 · 설계 §6.1).
///
/// <para>
/// <b>충돌 계산은 하나도 안 한다</b> — 모양을 들고만 있다(Area2D · monitoring 끔 · 레이어 0). 판정은 규칙 층이 하고,
/// 여기 놓이는 사각형은 규칙이 <b>이 틱에 실제로 대 본 것</b>이다(<see cref="BattleSim.BossTestedRects"/> 등).
/// 뷰가 hitboxes.json 을 따로 읽어 좌우를 뒤집고 발 위치를 더하면, 규칙 쪽 그 계산에 버그가 있어도 여기는
/// 멀쩡해 보인다 — 가장 필요한 순간에 거짓말하는 도구가 된다.
/// </para>
///
/// <para>
/// 판정을 Godot 물리로 옮기지 않는 이유: 학습 데이터는 봇이 <b>Godot 없이</b> 헤드리스로 수백만 판을 돌려
/// 만든다. 판정이 Area2D 에 있으면 판마다 엔진을 띄워야 하고, "같은 시드면 같은 결과" 도 물리 스텝에 묶인다.
/// </para>
/// </summary>
public partial class HitboxDebug : Node2D
{
    private static readonly Color _bossTested = new(1.0f, 0.2f, 0.2f, 0.45f);
    private static readonly Color _bossNext = new(1.0f, 0.7f, 0.1f, 0.15f);
    private static readonly Color _fighterTested = new(0.2f, 0.9f, 1.0f, 0.45f);
    private static readonly Color _bossBody = new(0.4f, 0.55f, 1.0f, 0.2f);
    private static readonly Color _fighterBody = new(0.3f, 1.0f, 0.4f, 0.25f);
    private static readonly Color _invulnerable = new(0.7f, 0.7f, 0.7f, 0.25f);
    private static readonly Color _parrying = new(1.0f, 0.95f, 0.2f, 0.35f);
    private static readonly Color _guarding = new(0.65f, 0.4f, 1.0f, 0.3f);

    private readonly List<CollisionShape2D> _pool = new();
    private Area2D _area = null!;
    private int _used;

    public override void _Ready()
    {
        // 스프라이트 위에 그린다 — 판정이 몸 뒤에 가려지면 보이는 것이 곧 맞는 것인지 볼 수 없다.
        ZIndex = 100;
        _area = new Area2D
        {
            Monitoring = false,
            Monitorable = false,
            CollisionLayer = 0,
            CollisionMask = 0,
        };
        AddChild(_area);
        Log.Debug("debug", "hitboxes=on");
    }

    /// <summary>파이터 몸통의 색 — 왜 안 맞았는지가 그 자리에서 보이게 한다.</summary>
    public static Color FighterColor(bool invulnerable, bool parrying, bool guarding) =>
        invulnerable ? _invulnerable
        : parrying ? _parrying
        : guarding ? _guarding
        : _fighterBody;

    /// <summary>이 프레임에 보일 사각형들을 놓는다. <b>규칙 좌표(위가 +)</b>로 받는다.</summary>
    public void Show(
        IReadOnlyList<HitRect> bossTested,
        IReadOnlyList<HitRect> bossNext,
        IReadOnlyList<HitRect> fighterTested,
        HitRect fighterBody,
        HitRect bossBody,
        Color fighterBodyColor)
    {
        _used = 0;
        Put(bossBody, _bossBody);
        Put(fighterBody, fighterBodyColor);
        foreach (HitRect r in bossNext)
        {
            Put(r, _bossNext);
        }

        foreach (HitRect r in bossTested)
        {
            Put(r, _bossTested);
        }

        foreach (HitRect r in fighterTested)
        {
            Put(r, _fighterTested);
        }

        for (int i = _used; i < _pool.Count; i++)
        {
            _pool[i].Visible = false;
        }
    }

    private void Put(HitRect r, Color color)
    {
        CollisionShape2D shape;
        if (_used < _pool.Count)
        {
            shape = _pool[_used];
        }
        else
        {
            shape = new CollisionShape2D { Shape = new RectangleShape2D() };
            _area.AddChild(shape);
            _pool.Add(shape);
        }

        _used++;

        ((RectangleShape2D)shape.Shape).Size = new Vector2((float)(r.X1 - r.X0), (float)(r.Y1 - r.Y0));
        // 규칙은 위가 + 이고 화면은 아래가 + 다 — FighterView 의 Position = (X, −Y) 와 같은 뒤집기다.
        shape.Position = new Vector2((float)((r.X0 + r.X1) / 2), (float)(-(r.Y0 + r.Y1) / 2));
        shape.DebugColor = color;
        shape.Visible = true;
    }
}
```

> 만약 `shape.DebugColor` 가 `CS1061` 로 빌드가 안 되면(이 Godot 버전에 속성이 없다), 그 한 줄을 지운다 — 사각형은 프로젝트의 기본 디버그 색으로 여전히 그려진다. 무엇을 지웠는지 PR 에 적는다.

- [ ] **Step 2: 전투가 켜졌을 때만 세우고 매 프레임 넘긴다**

`overfit/battle/Battle.cs`:

필드 — `private BattleResult _result = null!;` 근처 다른 뷰 필드들 옆에:

```csharp
    /// <summary>
    /// 판정 보기 (설계 §6.1). <b><c>--debug-collisions</c> 로 띄웠을 때만</b> 선다 — 아니면 null.
    /// Godot 의 Visible Collision Shapes 는 실행 중에 못 켠다(SceneTree.debug_collisions_hint 문서)라
    /// 켜고 띄웠을 때만 노드를 세운다. 안 켰으면 사각형을 옮기는 비용도 없다.
    /// </summary>
    private HitboxDebug? _hitboxDebug;
```

`_Ready` — `_bossView.Load(_bossConfig.Sprite);` 바로 아래:

```csharp
        if (GetTree().DebugCollisionsHint)
        {
            _hitboxDebug = new HitboxDebug();
            _world.AddChild(_hitboxDebug);
        }
```

`RenderFrame` — `_hud.Show(...)` 호출 뒤:

```csharp
        _hitboxDebug?.Show(
            _sim.BossTestedRects,
            _sim.BossNextRects,
            _sim.FighterTestedRects,
            _sim.Fighter.Body,
            _sim.Boss.Body,
            HitboxDebug.FighterColor(_sim.Fighter.Invulnerable, _sim.Fighter.Parrying, _sim.Fighter.Guarding));
```

(`Battle.cs` 에 `using Overfit.Battle.View;` 가 이미 있는지 본다 — 없으면 더한다.)

- [ ] **Step 3: `HITBOXES=1` 을 build.sh 에 둔다**

`tools/build.sh`:

머리 주석의 `LOG_LEVEL=trace tools/build.sh …` 줄 바로 아래:

```bash
#   HITBOXES=1 tools/build.sh run|shots  판정 보기 — Godot 의 Visible Collision Shapes 를 켠다(--debug-collisions).
#                                        규칙이 이 틱에 댄 판정 사각형이 그려진다. shots 는 그 사진을 docs/shots/ 로 안 넘긴다
```

`LOG_ARG` 두 줄 바로 아래:

```bash
# 판정 보기 (이슈 #59 · 설계 §6.1). 실행 중에는 못 켜므로(SceneTree.debug_collisions_hint) 띄울 때 정한다.
HITBOX_ARG=""
[[ "${HITBOXES:-}" == "1" ]] && HITBOX_ARG="--debug-collisions"
```

`cmd_run` 의 두 실행 줄:

```bash
  if [[ $# -gt 0 ]]; then "$GODOT" --path "$PROJECT" $HITBOX_ARG "$1" -- $LOG_ARG
  else "$GODOT" --path "$PROJECT" $HITBOX_ARG -- $LOG_ARG; fi
```

`cmd_shots` 의 실행 줄:

```bash
  "$GODOT" --path "$PROJECT" $HITBOX_ARG -- --shots "--shot-dir=$out" $LOG_ARG > "$log" 2>&1 || code=$?
```

`cmd_shots` 의 문서용 축소본 블록(`# 문서용 축소본.` 부터 `done` 까지)을 조건으로 감싼다:

```bash
  # 판정이 그려진 사진은 디버그용이다 — README 가 쓰는 docs/shots/ 로 넘기지 않는다.
  if [[ -n "$HITBOX_ARG" ]]; then
    ok "스크린샷 ${n}장 — $out (판정 보기라 docs/shots/ 는 그대로 둔다)"
    return
  fi

  # 문서용 축소본. 원본은 1920x1080 이라 README 에 그대로 넣으면 무겁다.
  mkdir -p "$ROOT/docs/shots"
  local f
  for f in "$out"/*.png; do
    sips -Z 960 "$f" --out "$ROOT/docs/shots/$(basename "$f")" >/dev/null 2>&1 || warn "축소 실패: $(basename "$f")"
  done
```

- [ ] **Step 4: `.uid` · 게이트**

Run: `tools/build.sh import && ls overfit/battle/view/HitboxDebug.cs.uid`
Expected: 파일이 있다

Run: `tools/build.sh check`
Expected: 0 warning · 0 error · 전부 통과 · `.uid 짝 — 전부 있음`

Run: `tools/build.sh smoke`
Expected: 통과 — `[E]` 0 · 엔진 `ERROR:` 0. (smoke 는 `--debug-collisions` 없이 돌므로 `HitboxDebug` 가 안 선다 — 안 켰을 때 아무것도 안 한다는 확인이다.)

- [ ] **Step 5: 커밋** (새 셸 호출)

```bash
git add overfit/battle/view/HitboxDebug.cs overfit/battle/view/HitboxDebug.cs.uid overfit/battle/Battle.cs tools/build.sh
git commit -m "판정 보기 — 규칙의 사각형을 Godot 의 Visible Collision Shapes 에 넘긴다 (#59)" \
  -m "보여주기는 Godot 의 기능을 쓴다 — 에디터 Debug 메뉴나 --debug-collisions 로 켜고, HITBOXES=1 tools/build.sh run|shots 가 그 인자를 붙인다. 실행 중에는 못 켜고 끈다(SceneTree.debug_collisions_hint 문서).

판정은 Godot 물리로 옮기지 않는다. 학습 데이터는 봇이 Godot 없이 헤드리스로 수백만 판을 돌려 만들고, 판정이 Area2D 에 있으면 판마다 엔진을 띄워야 하며 같은 시드면 같은 결과도 물리 스텝에 묶인다. 그래서 규칙이 이 틱에 댄 사각형을 CollisionShape2D 로 옮겨 놓기만 한다 — 충돌 계산은 하나도 안 하는 노드다." \
  -m "Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 눈으로 본다 · PR

- [ ] **Step 1: 판정이 그려진 사진을 찍는다**

Run: `HITBOXES=1 tools/build.sh shots`
Expected: `스크린샷 N장 — out/shots (판정 보기라 docs/shots/ 는 그대로 둔다)` · `git status --short docs/shots` 가 **비어 있다**

- [ ] **Step 2: 사진을 본다** — 이 설계의 전부가 "보이는 것이 곧 맞는 것" 이다

`out/shots/` 에서 접근 · 선딜 · 공격 장면 셋을 열어 본다. 확인할 것:

1. **초록 몸통이 파이터 그림 위에, 파란 몸통이 보스 그림 위에** 놓였나 — 발바닥이 바닥선에 닿나
2. **선딜 중 호박색 사각형 두 장**(옛 띠 = 좌우 대칭)이 보스 양옆에 있나
3. **파이터 칼(하늘색)** 이 공격 장면에서 보이나 — 옛 판정은 높이를 안 봐서 **세로로 긴 기둥**이다. 이상한 게 아니다; 그림의 모양으로 가는 것은 2번 계획이다
4. 대시 · 가드 장면이 있으면 파이터 몸통 색이 바뀌었나 (무적 = 회색 · 패리 창 = 노랑 · 가드 = 보라)

어느 하나라도 어긋나면 **멈추고 보고한다** — 판정과 그림이 어긋난 것이다.

- [ ] **Step 3: 끝 검사**

Run: `tools/build.sh check && tools/build.sh smoke && tools/build.sh demo`
Expected: 셋 다 통과

Run: `git ls-files overfit | grep -c '\.png$'`
Expected: `0`

Run: `tools/build.sh test 2>&1 | grep '규칙 테스트 통과'`
Expected: `규칙 테스트 통과 — M건` — 시작할 때 적은 N 에서 **새 테스트(13 + 7 + 6 + 2 + 5 = 33)** 만큼 늘었다 (Task 4 에서 옮기기만 한 테스트는 수가 안 바뀐다)

- [ ] **Step 4: 푸시 · PR**

```bash
git -c credential.helper='!gh auth git-credential' push -q https://github.com/Atralupus/OVERFIT.git feat/hit-shapes
gh pr create --head feat/hit-shapes --title "판정 모양 — 보스전 재설계 1/4 (#59)" --body "$(cat <<'P'
보스전 재설계(#59)의 첫 번째. 스펙 `docs/superpowers/specs/2026-09-24-보스전-재설계-design.md` §3 · §6.1 · §7.1.

**게임은 아직 옛 패턴(변종 아홉)으로 돈다.** 이 PR 은 그 위에 설 판정의 뼈대다.

## 무엇이 바뀌나

- **모든 판정이 몸통 대 모양이다.** 옛 판정은 파이터를 점(발 중심)으로 보고 거리를 띠와 견줬다. 이제 몸통(중심 ± 30 · 키 120)이 판정 모양(사각형 여러 장)과 겹치는지를 본다. 옛 띠는 좌우 대칭 두 장, 옛 공격 사거리는 높이를 안 보는 한 장으로 옮겨 같은 통로를 탄다.
- **판정은 창 동안 산다.** 한 번 휘두르면 한 번만 맞고, 대시 무적이 창보다 먼저 풀리면 그 뒤 틱에 맞는다. 옛 패턴은 창이 한 틱이라 이 변경만으로는 골든이 한 글자도 안 움직였다.
- **그림에서 판정 모양을 뽑는다** — `tools/extract_hitboxes.py` → `overfit/data/hitboxes.json`. 기준점은 `.tres` 의 region(아래끝 = 발바닥). PNG 는 커밋하지 않는다.
- **빗나간 이유가 셋이다** — 멀어서 · 넘어서 · **틈에 서서**. `MissedTooClose` 는 `MissedByGap` 이 됐다.
- **판정 보기** — `HITBOXES=1 tools/build.sh run`. Godot 의 Visible Collision Shapes 를 쓰고, 규칙이 이 틱에 **실제로 댄** 사각형을 그린다.

## 골든

<옛 줄> → <새 줄>. 띠의 양 끝이 몸 반폭(30)만큼 넓어진 셈이라 경계의 판정이 바뀌었고, 다이제스트가 `MissedByGap` 이름을 싣는다.

## 옮긴 테스트

<N>개 — 전부 파이터가 판정 가장자리 30px 안에 서 있던 것이다. 파이터를 정확히 30px 옮겼고 단언은 안 고쳤다.

## 확인

- `tools/build.sh check` — 0 warning · 0 error · 규칙 <M>건
- `smoke` · `demo` 통과 · 데모 로그의 모든 `[dodge]` 줄을 기하와 대조
- `HITBOXES=1 tools/build.sh shots` — 몸통 · 판정이 그림 위에 맞게 놓였다
- `git ls-files overfit | grep -c '\.png$'` → 0

🤖 Generated with [Claude Code](https://claude.com/claude-code)
P
)"
```

(`<옛 줄>` · `<새 줄>` · `<N>` · `<M>` 을 채운다. **머지하지 않는다.**)

---

## Self-Review

**1. 스펙 커버리지** — 이 계획의 범위(스펙 §10 의 1번):

| 스펙 | 태스크 |
|---|---|
| §3.1 격자 모양 | Task 1 (`HitShape`) · Task 2 (도구) |
| §3.2 도구 · 기준점 · id 규약 | Task 2 |
| §3.3 `hitboxes.json` · 커밋 · 테스트 불변식 | Task 2 |
| §3.4 몸통 (보스 키 297) | Task 4 |
| §3.5 판정 규칙 1~4 · 틱 반올림 | Task 3 (창 · 한 번만 · 관측 하나 · `TicksFor`) · Task 4 (몸통 대 모양) |
| §6.1 판정 보기 · 규칙의 사각형을 그린다 · 켰을 때만 · 로그 | Task 5 · Task 6 |
| §7.1 멀어서 · 넘어서 · 틈 · `MissedTooClose` 흡수 | Task 1 (`ShapeContact`) · Task 4 (`MissedByGap`) |
| §9.5 사진으로 확인 | Task 7 |

범위 밖(2·3·4번 계획): 2연격 · 가드/패리 분리 · 보스 두 패턴 · 점프 · 링 제거 · 계측의 차지 칸 제거.

**2. 자리 표시 검사** — 코드 단계는 전부 코드를 담는다. 옛 코드를 옮기라는 두 곳(Task 3 `Land`, Task 5 `Strike`)은 무엇을 옮기고 무엇만 바꾸는지를 정확히 적었다. PR 설명의 `<…>` 네 칸은 실행해서 나오는 값이다.

**3. 타입 일관성** — `HitRect(X0, X1, Y0, Y1)` · `Placement(X, Y, Facing)` · `ShapeHit.Test(HitShape, Placement, HitRect)` · `HitShape.Place(Placement)` · `HitShape.ToWorld(HitRect, Placement)` · `HitBox(HitShape Shape, int Damage, …, double ActiveSeconds)` · `PatternRunner.ShapeOf(PatternStep)` · `BattleSim.TicksFor(double)` · `Step(LiveSwing, Placement)`(Task 5 에서 `Placement` 인자가 붙는다 — Task 3 에서는 없다) · `BossTestedRects` / `BossNextRects` / `FighterTestedRects` — 모든 태스크에서 같은 이름이다.

**4. Review Focus** — 다섯 줄 모두 테스트가 소유 태스크에 들어 있다 (위 표의 태스크 번호대로).
