# 고정 시드 · 고정 입력 시퀀스로 돌린 한 판의 결과.
# 이 값이 달라지면 결정론이 깨진 것이다 — 규칙을 되돌리거나, 일부러 바꿨다면
# 무엇을 왜 바꿨는지 PR 에 적고 이 값을 갱신한다.
#
# 형식: <시드> <틱수> <파이터HP> <보스HP> <관측수> <관측다이제스트>
#
# 마지막 칸이 회피 관측 스트림 전체(패턴·verb·판정·타이밍·방향·공중·거리·욕심·가능했던 수단)의 FNV-1a 다.
# 앞 네 숫자만 박아두던 때는 계측이 통째로 골든 밖이었다 — 부호를 뒤집든 기준 시각을 한 틱
# 옮기든 초록이었고, 그 사이 망이 먹을 특징은 전부 달라졌다. 10축은 이 스트림의 집계이므로
# 스트림을 덮으면 축도 덮인다.
#
# 2026-09-22 갱신 (이슈 #22 · 공중 몸 통과): 앞 네 숫자(틱수 1586 · 파이터HP 52 · 보스HP 0 · 관측 16)는
# 그대로고 **다이제스트만** 0d8bbf18769f93b5 → 429f3f56ce3a4c9d 로 움직였다. 몸 충돌을 지상 전용으로
# 바꿔 공중에서 보스를 가로지를 수 있게 했고, 그래서 판정이 설 때의 거리(DodgeEvent.Distance)가
# 달라졌다 — 판의 겉모습이 아니라 계측이 움직인 것이다. 골든이 네 숫자만 박아뒀다면 이 변경은
# 조용히 초록이었을 것이다.
#
# 2026-09-22 갱신 (이슈 #26 · 에셋 교체): 다이제스트가 429f3f56ce3a4c9d → e02ab5ea36e7bbc3 로
# 움직였다. 앞 네 숫자(틱수 1586 · 파이터HP 52 · 보스HP 0 · 관측 16)는 그대로다 —
# 보스 스프라이트가 Duelyst 4배에서 Medieval King 5.5배로 바뀌면서 히트박스 반폭을
# 120 → 85 로 같이 옮겼고(bosses.json), 판정이 설 때의 거리(DodgeEvent.Distance)가 그만큼 달라졌다.
# 반폭만 안 옮겼으면 보스의 사거리가 몸과 다른 말을 했을 것이고, 이 줄은 그대로였을 것이다.
# 2026-09-23 갱신 (이슈 #27 · 몸 충돌 제거): 틱수 1586 → 1654 · 다이제스트 e02ab5ea36e7bbc3 →
# 643a1e18e87db7d2. 파이터가 보스를 그냥 지나가게 되면서 판정이 설 때의 거리가 달라졌고,
# 그만큼 맞는 판정도 달라져 판이 68틱 길어졌다. 파이터HP(52) · 보스HP(0) · 관측수(16)는 그대로다.
# 2026-09-23 갱신 (이슈 #27 · 대시 사거리 · 점프 정점): 51 1654 52 0 16 643a1e18e87db7d2 →
# 51 1338 0 8 14 24d4340b2f36bfd7. 판이 **뒤집혔다** — 고정 입력 스크립트는 23틱마다 무조건
# 대시하는 멍청이라, 대시가 183 → 367px 로 늘자 보스 사거리 밖으로 튕겨 나가 칼이 덜 닿는다.
# 밸런스 판정이 아니라 스크립트의 성질이다(사람과 봇은 거리를 보고 누른다).
# 2026-09-23 갱신 (이슈 #28 · 백장 세 패턴): 51 1338 0 8 14 24d4340b2f36bfd7 →
# 51 1245 0 96 19 b93a9f6c74c3cd9b. **여섯 칸이 전부 움직였다** — 당연하다. 이 판이 도는 패턴이
# 다른 패턴이다(일곱 → 셋, stages.json 5단계 명부도 통째로). 판이 93틱 짧아지고 보스 체력이
# 8 → 96 으로 많이 남은 것은 고정 입력 스크립트가 **17틱마다 무조건 공격하는 멍청이**이기 때문이다:
# 백장의 패턴은 셋 다 연속타라 선딜 사이의 빈 시간이 줄었고, 그만큼 스크립트의 공격이 사거리
# 밖에서 헛돈다. 관측이 14 → 19 로 는 것도 같은 이유다(multi_hit 3 + 2 + 2 = 판당 판정이 많다).
# 밸런스 판정이 아니라 스크립트의 성질이다 — 사람과 봇은 거리를 보고 누른다.
# 2026-09-23 **안 움직였다** (이슈 #38 · 캐릭터 하나 · 공격 타이밍): 51 1245 0 96 19 b93a9f6c74c3cd9b 그대로다.
# fighters.json 을 셋에서 하나로 줄이고 그 하나의 공격 시간을 0.30 → 0.50초로 바꿨는데도 여섯 칸이
# 다 같은 이유는 **이 골든이 fighters.json 을 안 읽기 때문**이다 — TestConfigs.Fighter() 라는 손으로
# 세운 기준값으로 돈다(그 파일의 주석이 이유를 적어 뒀다: 캐릭터 밸런스를 고칠 때마다 무관한
# 테스트가 같이 빨개지지 않게). 즉 **이 골든은 결정론을 덮지 캐릭터 수치를 안 덮는다.**
# 나중에 "밸런스를 고쳤는데 골든이 그대로다" 를 보면 깨진 것이 아니라 이 성질이다. 캐릭터 수치는
# FighterDataTests 가 따로 본다(공격 액션이 애니메이션 길이와 같은가 · 대시가 보스 몸을 지나는가).
# 2026-09-23 갱신 (이슈 #40 · 차지 공격): 51 1245 0 96 19 b93a9f6c74c3cd9b →
# 51 1245 0 96 19 fcde63148abec1f7. **다이제스트만 움직였다** — 앞 다섯 칸이 한 자리도 안 변한 것이
# 이 변경의 증거다. 입력 계약에 누름 유지(InputFrame.AttackHeld)를 더했는데 이 골든의 스크립트는
# 그 칸을 안 적는다(기본 false = 탭). 그래서 **한 번도 안 모으고**, 차지가 없으면 판은 예전 그대로다 —
# 만약 틱수나 체력이 움직였다면 그냥 누른 공격의 뜻이 바뀐 것이고, 그건 되돌려야 할 회귀였다.
# 다이제스트가 움직인 이유는 관측에 칸이 하나 늘었기 때문이다(DodgeEvent.ChargeTier, 값은 전부 0).
# 값이 0 뿐인데도 넣는 이유는 이 파일 맨 위에 적힌 그대로다: 스트림에서 빠진 필드는 골든 밖이라,
# 배수 규칙을 통째로 갈아도 초록이 된다. greed 도 이번에 차지를 세게 됐지만(BattleSim) 이 스크립트는
# 모으지 않아 그 값도 안 움직였다 — 계측이 바뀐 것이 판을 바꾸지 않았다는 뜻이다.
# 2026-09-23 **안 움직였다** (이슈 #40 · 붙드는 것이 곧 선딜): 51 1245 0 96 19 fcde63148abec1f7 그대로다.
# 차지를 놓을 때 선딜을 처음부터 다시 기다리지 않고 **붙든 만큼 깎아서** 기다리게 고쳤는데
# (max(0, attack_windup - 붙든 시간)), 이 골든이 그대로인 것이 그 변경이 옳게 좁다는 증거다:
# 스크립트는 한 번도 안 붙들므로 깎일 것이 0 이고, 그래서 그냥 누른 칼질은 선딜도 판정도 길이도
# 예전과 같다. 여기가 움직였다면 붙들지 않은 공격까지 빨라진 것이고, 그건 이 이슈가 요청한 적 없는
# 밸런스 변경이었을 것이다. 봇은 이 골든에 안 탄다(고정 입력 스크립트다) — 봇의 차지는
# BotPolicyTests 가 따로 본다.
# 2026-09-24 갱신 (이슈 #46 · 계측이 거짓말한다): 51 1245 0 96 19 fcde63148abec1f7 →
# 51 1245 0 96 19 3f2273bfbbda7436. **다이제스트만 움직였다** — 앞 다섯 칸이 한 자리도 안 변한 것이
# 이 변경이 판이 아니라 계측을 고쳤다는 증거다. 움직인 이유는 `MissedByRange` 를
# `MissedTooFar` · `MissedTooClose` 로 가른 것 하나뿐이다: 이 판의 관측 19건 중 8건이 거리로 빗나갔고
# (7건은 밖 · 1건은 점프 강타의 안쪽 주머니 165px), 그 여덟 줄의 판정 이름이 스트림에서 바뀌었다.
# 여기서 틱수나 체력이 움직였다면 판정 순서나 기하를 건드린 것이고, 그건 이 이슈가 요청한 적 없는
# 변경이었을 것이다 — 이 이슈는 "무엇이 일어났나" 가 아니라 "그것을 무엇이라 적나" 만 고친다.
#
# verb 는 **한 건도 안 바뀌었다.** 같은 이슈가 "대시가 만든 거리는 대시의 공" 도 같이 넣었는데
# 이 골든은 그대로인 것이, 그 규칙이 옳게 좁다는 증거다: 고정 입력 스크립트는 23틱마다 무조건
# 대시하는 멍청이라 대시를 시작한 자리가 이미 사거리 밖(437~451px 대 사거리 250)이고,
# 그러면 그 거리를 만든 것은 대시가 아니라 서 있던 자리다. 공을 돌리는 조건이 "대시 중" 이었다면
# 이 여덟 줄이 전부 Dash 로 뒤집혔을 것이고, 그건 계측을 반대 방향으로 거짓말시키는 것이다.
# **데모(out/demo.log · 시드 51)도 verb 가 한 건도 안 움직였다** (23건 중 거리 miss 7건 — 밖 3 · 안 4).
# 이유는 다르다: BotPolicy 는 늘 보스를 보고 서 있어 대시가 **항상 안쪽**이고(dir=+1 셋),
# 판정 0.10초 안에서만 누르므로 무적이 닫히기 전에 판정이 선다 — 거리로 빠질 틈이 없다.
# 즉 **이 이슈의 버그를 최소 봇은 한 번도 안 밟는다.** 밟는 쪽은 코앞에 서 있다 밖으로 뛰는 사람이고,
# 그 판은 BattleSimTests.밖으로_뛰는_사람이_간격_의존자로_안_읽힌다 가 진짜 패턴으로 돌린다:
# 같은 시드 51 에서 대시 1 · 간격 15 → 대시 7 · 간격 9 다(19건 중 6건이 뒤집힌다).
# 2026-09-24 **안 움직였다** (이슈 #47 · 가드): 51 1245 0 96 19 3f2273bfbbda7436 그대로다.
# 입력 계약에 누름 유지가 한 칸 늘었는데(InputFrame.ParryHeld) 이 스크립트는 그 칸을 안 적는다
# (기본 false = 탭). 그래서 **가드가 한 번도 안 서고**, 안 서면 판은 예전 그대로다 — AttackHeld 를
# 더했을 때(#40)와 같은 자리다. 규칙 쪽에서 움직일 수 있었던 것은 하나뿐이었다: `내려찍기 3연` 의
# 마지막 판정에 guard_break 를 달아, 그것을 **정확 패리**로 받으면 보스 경직이 0.5 → 1.6초가 된다.
# 이 판에는 정확 패리가 한 건도 없어서(스트림의 verb 는 Spacing · Jump · None 뿐이다) 안 걸렸다.
# 여기가 움직였다면 가드가 안 서는 판의 무언가를 같이 바꾼 것이고, 그건 되돌려야 할 회귀였다.
#
# **붙들어 보고 안 붙들기로 했다.** Script() 에 `ParryHeld: i % 29 < 26` 을 넣으면
# 51 1245 0 96 19 3f2273bfbbda7436 → 51 1032 0 88 16 31523309a0eb1692 로 여섯 칸이 움직이는데,
# 그렇게 얻은 스트림에 **Guard 는 한 건도 없다**(Spacing 7 · Jump 3 · None 6). 이유는 스크립트의
# 성질이다: 17틱마다 공격(액션 17틱) · 23틱마다 대시(11틱)라 29틱마다 오는 패리 누름이 거의 언제나
# 행동 중에 떨어져 버려지고, 어쩌다 선 가드는 파이터를 세워 둘 뿐 판정을 받지 않는다.
# 즉 **판만 달라지고 덮이는 것은 없다** — 골든을 그만큼 비싸게 다시 박을 이유가 아니다.
# 가드가 이 스크립트에 실리려면 누름 자리를 다시 설계해야 하는데, 그건 이 파일의 역사 전체가
# 기대고 있는 시퀀스를 갈아엎는 일이라 이 이슈의 범위가 아니다(이슈 #48 이 변종을 세울 때 같이 본다).
# 그때까지 가드의 회귀는 FighterActionTests · HitResolverTests · BattleSimTests 가 보고,
# 같은 시드가 같은 판을 내는지는 BotPolicyTests 가 본다 — 그 봇은 실제로 가드를 낸다.
# 2026-09-24 갱신 (이슈 #48 · 내려찍기 단일 계열): 51 1245 0 96 19 3f2273bfbbda7436 →
# 51 2320 0 120 34 17f7adf8375a4f71. **여섯 칸이 전부 움직였다** — #28 때와 같은 이유이고,
# 당연하다: 이 판이 도는 패턴이 **다른 패턴**이다. 보스가 계열 하나(내려찍기)로 줄고 그 안의
# 변종 아홉이 들어왔으며, stages.json 도 5단계에서 3단계로 바뀌었다. 이 골든이 도는 명부도
# 5단계(실제로는 잘려서 3단계였다)에서 3단계로 옮겼다 — 이제 그것이 마지막 단계다.
#
# 무엇이 어디서 왔는지:
#   틱수 1245 → 2320   패턴이 길어졌다(1.8~1.9 → 2.9~3.25초). 선딜 0.85 · 간격 0.55 · 0.90 은
#                      느리게 만든 것이 아니라 **네 답이 다 성립하게** 만든 값이다(설계 §2.2):
#                      간격 0.35 는 parry_lock(0.6167)보다 짧아 한 번 늦으면 세 번 벌을 받았고,
#                      선딜 0.50 은 tell_lead(0.9)보다 짧아 예고가 44% 크기로 튀어나왔다.
#   보스HP 96 → 120    고정 입력 스크립트는 17틱마다 무조건 휘두르는 멍청이라, 보스가 패턴에
#                      머무는 시간이 길어진 만큼 사거리 밖에서 헛돈다. 밸런스 판정이 아니라
#                      스크립트의 성질이다(사람과 봇은 거리를 보고 누른다).
#   관측 19 → 34       판당 판정이 3~4개인 변종만 남았고 판도 길어졌다.
#   파이터HP 0         안 변했다. 이 스크립트는 여전히 맞아 죽는다.
#
# 스트림의 속을 같이 적어 둔다 — 다음에 이 줄이 움직일 때 무엇을 견줄지가 여기 있다:
#   verb    Spacing 23 · None 8 · Jump 3 (Dash 0)
#   verdict MissedTooFar 21 · Hit 11 · MissedTooClose 2
#   변종    III-역습 13 · III-끌기 12 · III-역린 6 · III-쇄도 3 (III-쐐기는 이 시드에 안 뽑혔다)
#
# 세 가지가 이 갱신에서 **증인**이다.
# ① Dash 가 0 이고 Spacing 이 23 인 것은 #46 때와 같은 이유다 — 이 스크립트는 사거리 밖에서
#    대시하므로 그 거리를 만든 것이 대시가 아니다. 계열이 바뀌어도 그 판단은 그대로 서 있다.
# ② MissedTooClose 2 는 **III-끌기의 안쪽 주머니**(290px)에 들어간 것이다. 점프 강타가 지던
#    그 일을 끌기가 물려받았다는 증거이고, 여기가 0 이 되면 distance_bias 축이 조용히 죽는다.
# ③ 헛스윙이 이 판에 2번 지나갔는데 **관측은 한 건도 안 늘었다**(III-역린 6건은 전부 판정이다).
#    feint 를 damage 0 판정으로 흉내 냈다면 여기가 36 이었을 것이고, 일어난 적 없는 판정 둘이
#    학습 데이터에 섞였을 것이다 — kind 를 새로 만든 이유가 이 두 줄 차이다.
# 2026-09-24 갱신 (이슈 #53 · 패리와 가드를 하나로): 51 2320 0 120 34 17f7adf8375a4f71 →
# 51 1440 0 176 20 2d1bb9d0f1002a2a. **여섯 칸이 전부 움직였다.** 이 골든의 스크립트는 29틱마다
# 패리를 탭하는데, 그 탭의 **뜻이 바뀌었다** — 전에는 0.30초짜리 패리 행동(18틱)이 서서 그동안
# 걷지도 대시하지도 공격하지도 못했고, 지금은 누른 그 틱에 방어 자세가 서고 다음 틱에 손을 떼는
# 순간 풀린다(이슈 #53). 즉 이 멍청이가 **17틱 더 자유로워졌다.**
#
# 무엇이 어디서 왔는지:
#   틱수 2320 → 1440   자유로워진 만큼 더 자주 대시하고 더 자주 보스 사거리 안으로 들어간다.
#                      맞는 판정이 늘어 **더 빨리 죽는다** — 판이 880틱 짧아진 것이 그것이다.
#   보스HP 120 → 176   판이 짧아진 만큼 17틱마다 나가는 헛스윙도 덜 닿았다.
#   관측 34 → 20       판이 짧으면 지나가는 판정도 적다.
#   파이터HP 0         안 변했다. 이 스크립트는 여전히 맞아 죽는다.
#
# 스트림의 속:
#   verb    Spacing 10 · Jump 6 · None 4 (Dash 0 · Guard 0 · Parry 0)
#   verdict MissedTooFar 10 · Hit 10
#   변종    III-끌기 6 · III-역린 6 · III-역습 5 · III-쇄도 3 (III-쐐기는 이 시드에 안 뽑혔다)
#
# ⚠ **MissedTooClose 가 2 → 0 이 됐다.** 앞 갱신이 "여기가 0 이 되면 distance_bias 축이 조용히
# 죽는다" 고 적어 둔 그 칸이다. 죽은 것은 축이 아니라 **이 스크립트가 그 자리를 안 밟는 것**이고,
# 이유는 위와 같다: 패리 탭이 발을 묶지 않게 되면서 걸음의 궤적이 통째로 달라져 III-끌기 의
# 안쪽 주머니(290px)에 서 있는 일이 이 1440틱 안에 한 번도 안 일어났다. 축과 판정 자체는 그대로고
# (HitResolverTests.거리로_빗나간_것이_안인지_밖인지까지_말한다 가 본다) 봇의 판도 그대로 밟는다.
# 스크립트를 고쳐 되살리지 않는 이유는 이 파일의 역사 전체가 그 시퀀스에 기대고 있기 때문이다.
#
# **Guard 도 Parry 도 0 이다** — 그것도 스크립트의 성질이다. ParryHeld 를 안 적으므로 자세는
# 한 틱만 서고, 29틱 주기의 탭이 판정 창(0.133초 = 8틱)에 겹치는 일이 이 판에 한 번도 없었다.
# 방어의 회귀는 FighterActionTests · HitResolverTests · BattleSimTests · BotPolicyTests 가 보고,
# 그중 BotPolicyTests 의 봇은 실제로 패리도 가드도 낸다.
#
# **박자가 고정됐다는 것은 여기 말고 두 테스트가 잰다.**
# BattleSimTests.연속타의_박자는_패리를_해도_그대로다 가 합성 패턴으로 규칙을 못박고,
# BattleSimTests.실제_1단계_패턴의_2타에서_3타까지가_패리와_무관하다 가 **진짜 `내려찍기 I`** 로
# 같은 것을 잰다: 2타 → 3타가 패리해도 안 해도 **55틱**이다. 옛 규칙(정확 패리 = 경직 0.5초)으로
# 같은 것을 재면 **85틱**이고(실제로 되돌려 재 봤다), 뷰의 히트스톱 7프레임이 더해져 사람이
# 겪는 것은 92프레임(1.53초)이었다 — 유저가 "딜레이가 매번 다르다" 고 한 것이 그 30틱이다.
# 2026-09-24 **안 움직였다** (이슈 #53 · 상을 마무리에 걸다): 51 1440 0 176 20 2d1bb9d0f1002a2a 그대로다.
# 같은 이슈 안에서 경직의 조건을 `guard_break` 에서 **`finisher`(패턴의 마지막 판정)** 으로 옮겼는데
# 이 줄이 한 자리도 안 움직인 것이, 그 변경이 **패리를 통해서만 들어간다**는 증거다:
# 위에 적힌 대로 이 스크립트의 스트림에는 Parry 가 0 건이라 경직이 걸릴 자리가 아예 없다.
# 여기가 움직였다면 받아치지 않은 판정까지 보스를 굳힌 것이고, 그건 되돌려야 할 회귀였다.
# 그 변경이 실제로 일하는지는 BattleSimTests.마무리를_받아쳤을_때만_보스가_굳는다 ·
# 상은_손으로_단_깃발이_아니라_마무리_자리에_걸린다(같은 PR 에서 이름을 바꿨다) 가 본다 —
# 봇의 판(demo)도 1·3단계에서 같이 돌렸다.
# 2026-09-24 갱신 (이슈 #53 · 관측의 두 칸을 다이제스트에 넣다 · 마무리 전부 가드 불가):
# 51 1440 0 176 20 2d1bb9d0f1002a2a → 51 1440 0 176 20 432b84d8a7ce5a58. **다이제스트만 움직였다.**
# 움직인 것은 규칙이 아니라 **다이제스트가 보는 칸**이다: 이 이슈가 DodgeEvent 에 더한 두 칸
# (GuardAvailable · Finisher)을 더할 때 여기 넣는 것을 빠뜨렸다. 이 파일 맨 위의 규약 그대로
# 스트림에서 빠진 필드는 골든 밖이라, 그동안은 마무리 판정을 통째로 옮기거나 한 변종에서
# guard_break 를 빼도 이 줄이 초록이었다. 이제 두 칸이 들어가 있다 — 이 판 20건 중 마무리 6 ·
# 가드 불가 6 이고 둘이 **같은 여섯 건**이다(가드 불가 ⟺ 마무리 · PatternDataTests 가 양쪽에서
# 못박는다). 마무리 여섯의 판정은 Hit 4 · MissedTooFar 2 — 이 스크립트는 패리를 한 번도 못 한다.
#
# 같은 갱신에 들어간 **데이터 변경은 이 줄을 한 자리도 안 움직였다.** 순서를 일부러 갈라 돌렸다:
# 데이터를 먼저 바꾸고 옛 다이제스트로 돌렸더니 2d1bb9d0f1002a2a 그대로였고, 그 뒤에 두 칸을
# 넣어 432b84d8a7ce5a58 이 됐다. 그래서 이 줄의 움직임은 전부 새로 넣은 두 칸의 몫이다.
#   ① 마무리를 아홉 변종 전부 가드 불가로 (유저 결정 · 빨강 = 가드로 못 막는다 = 받아쳐라).
#      이 골든은 3단계 명부로 도는데 3단계 다섯은 원래부터 전부 가드 불가였다 —
#      1단계 · 2단계 넷에 새로 붙인 깃발은 이 판 밖이다.
#   ② III-쐐기 의 셋째 8 → 32 · 마무리 26 → 14 (붙든 가드를 빨강 전에 깬다). 이 시드에는
#      III-쐐기 가 안 뽑힌다(앞 갱신들 그대로).
# 그 둘이 실제로 일하는지는 이 줄이 아니라 PatternDataTests(모든_마무리는_가드_불가다 ·
# 셋째_단계_변종은_둘째_단계_짝과_가드_불가_말고도_다르다)와 WedgeSealTests 가 본다.
# 2026-09-24 갱신 (이슈 #54 · 감각 조정 넷): 51 1440 0 176 20 432b84d8a7ce5a58 →
# 51 1840 0 200 25 e552749d39854b0c. **파이터HP 를 뺀 다섯 칸이 움직였고, 전부 박자 하나에서 왔다.**
# 이 이슈의 넷 중 이 줄에 닿는 것은 셋째(보스 간격을 넓힌다)뿐이다:
#   · 공격 선딜 0.3333 → 0.0833 · 차지 배수 ×2.5 · ×5 · 붕괴 고정 0.9 → 1.1 은 fighters.json 이고, 이 골든은
#     그 파일을 안 읽는다(TestConfigs.Fighter() 기준값으로 돈다 — #38 갱신에 적힌 성질 그대로다).
#     붕괴 고정만은 기준값도 같이 옮겼다(가드 셋은 기준값이 실제 값 그대로여야 한다 — TestConfigs 의 주석).
#     그래도 이 줄은 안 움직였다: 고정을 0.9 와 1.1 로 각각 돌려 **같은 여섯 칸**을 확인했다 — 이 스크립트는
#     ParryHeld 를 안 적어 가드를 붙들지 않으므로 깨질 가드가 없다.
#   · 디버그 단계 점프(1 · 2 · 3)는 뷰 쪽이라 규칙 층에 없다.
#
# 무엇이 어디서 왔는지 — 전부 patterns.json 이다. 1단계 간격 0.55 · 0.90 → 0.70 · 1.10 (판정 0.85 / 1.55 / 2.65),
# 박자가 곧 정체성인 넷은 비례로 옮겼다(끌기 2.83 · 쐐기 1.42 / 1.99 / 3.09 · 역린 헛스윙 2.28 · 역습 미끼 1.75):
#   틱수 1440 → 1840   판정 사이가 넓어져 같은 시간에 오는 판정이 줄었다 — 이 멍청이는 더 천천히 죽는다.
#                      패턴 길이가 2.65~3.25 → 3.00~3.69초다.
#   보스HP 176 → 200   17틱마다 휘두르는 칼이 **한 번도 안 닿았다**(전에는 세 번). 보스가 패턴에 머무는
#                      시간이 늘어 따라오지 않는 동안, 이 스크립트는 제멋대로 걷고 뛰며 사거리 밖에서 헛돈다 —
#                      밸런스 판정이 아니라 스크립트의 성질이다(사람과 봇은 거리를 보고 누른다).
#   관측 20 → 25       판이 400틱 길다.
#   파이터HP 0         안 변했다. 이 스크립트는 여전히 맞아 죽는다.
#
# 스트림의 속:
#   verb    Spacing 14 · None 8 · Parry 2 · Jump 1 (Dash 0 · Guard 0)
#   verdict MissedTooFar 14 · Hit 10 · Parried 1
#   변종    III-역습 10 · III-끌기 6 · III-역린 6 · III-쇄도 3 (III-쐐기는 이 시드에 안 뽑혔다 — 앞 갱신들 그대로)
#   마무리 7 = 가드 불가 7 (같은 일곱 건) · 그 판정은 Hit 4 · MissedTooFar 3
#
# ⚠ **Parried 가 0 → 1 이 됐다.** 29틱 주기의 패리 탭이 판정 창(8틱)에 이 판에서 처음 겹쳤다 — 박자가
# 바뀌면 그 겹침도 바뀐다. 받은 것은 마무리가 아니라(마무리 일곱의 판정은 Hit · MissedTooFar 뿐) 보스는
# 안 굳었다. 상이 마무리에만 걸린다는 규칙(이슈 #53)이 이 판에서도 그대로라는 증인이다.
#
# 박자가 변종의 정체성을 지키는지는 이 줄이 아니라 VariantRhythmTests 가 1단계의 실제 데이터와 맞대어 본다
# (역습의 미끼는 2타 직후의 칼질을 친다 · 끌기는 1단계 박자의 대시 착지를 친다 · 쐐기의 마무리 앞 간격 ·
# 역린의 두 창 · 쐐기가 깬 가드는 빨강까지 굳어 있다). 2타 → 3타가 패리와 무관하게 같은지는
# BattleSimTests 가 잰다: **55틱 → 66틱**(1.10초)이다.
51 1840 0 200 25 e552749d39854b0c
