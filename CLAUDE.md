# CLAUDE.md

Claude 와 사람이 **같이** 읽는 규칙이다. 여기 적힌 것은 취향이 아니라 계약이다 —
대부분은 게이트(`tools/build.sh check`)나 컴파일러가 강제한다.

> 이 저장소는 **베이스만 서 있다.** 게임은 아직 없다.
> 아래 규칙은 첫 줄의 게임 코드가 들어올 때를 위해 미리 정해둔 것이다.

---

## 0. 작업 흐름

- **main 에 직접 커밋하지 않는다.** 모든 변경은 브랜치 → PR 을 거친다.
  커밋 훅(`tools/precommit_check.sh`)이 막는다. 우회는 커밋 메시지에 `on-main: <사유>`.
- **규칙 코드는 TDD 로 고친다.** 실패하는 테스트 먼저(red) → 통과할 만큼만(green) → 정리(refactor).
  범위는 손으로 적지 않는다 — `tests/Overfit.Rules.Tests` 의 csproj 가 **링크하는 파일**이 곧 범위다.
  우회는 `no-test: <사유>`.
- 커밋 전 게이트는 `tools/build.sh check` 하나다. **검증하는 곳이 늘어나도 그 명령을 부른다** —
  단계를 딴 데 복사해 두면 게이트가 자랄 때마다 조용히 어긋난다(복사본만 짧아진다).

## 1. 규칙과 그림을 나눈다

게임 규칙은 **Godot 을 모르는 순수 C#** 이다. 씬은 그 결과를 그릴 뿐이다.

```
overfit/core/Log.cs        순수 — Godot 을 모른다
overfit/core/LogSink.cs    Godot 절반 — GD.Print 와 OS.* 가 여기 있다
```

이 경계를 **컴파일러가 강제한다.** `tests/Overfit.Rules.Tests` 의 csproj 가 순수 파일을 링크하는데,
그 어셈블리에는 GodotSharp 참조가 없다. Godot 을 끌고 오는 파일이 하나라도 섞이면 즉시 `CS0246` 이다.

새 순수 파일을 만들면 **그 csproj 의 링크 목록에 손으로 한 줄 넣는다.** 와일드카드를 쓰지 않는 이유는
그 목록이 곧 "무엇이 규칙인가"의 선언이기 때문이다 — TDD 게이트도 이 목록을 읽는다.

Godot 쪽 클래스(`Node` 를 상속하는 것)는 **파일 이름과 클래스 이름이 같아야 한다.**
어긋나면 빌드는 통과하고 **런타임에만** 스크립트가 안 붙는다.

## 2. 종류가 늘어나는 곳에 분기를 두지 않는다

`switch` 가 길어지는 자리는 전부 **인터페이스 + id→구현 등록표 + JSON 수치** 넷으로 만든다.
하나 더할 때 기존 파일을 열지 않는 것이 목표다.

## 3. 수치는 코드가 아니라 데이터에

`overfit/data/*.json` 이 진실이다. C# 에 매직 넘버를 두지 않는다.
DTO 는 `core/BalanceData.cs` 이고, `required` 가 붙은 키가 빠지면 부팅이 **빠진 키를 전부 나열하고** 멈춘다.

제약(예: "이 값의 합은 저 값보다 작아야 한다")이 생기면 `tools/verify_balance.py` 를 만들어
`cmd_check` 에 한 줄로 붙인다. 아직 제약이 없어 그 검사기는 없다.

## 4. 같은 시드면 같은 결과

난수는 "뽑는" 것이 아니라 **시드·도메인·키로 좌표를 조회**하는 것이다 (`core/Det.cs`).
호출 순서가 값에 섞이지 않으므로, 무엇을 언제 굴렸는지와 무관하게 같은 입력은 같은 결과다.

- 규칙 층에 `System.Random` 도 벽시계도 두지 않는다.
- `Det` 의 상수와 연산 순서는 **계약이다.** 하나만 바꿔도 모든 리플레이가 달라진다.
  `tests/…/Core/DetTests.cs` 의 골든 벡터가 막는다 — 그 값들은 파이썬으로 알고리즘을 **따로 구현해** 얻은 것이다.
- 새 난수 스트림은 `Det.Domain` 에 **번호를 추가만** 한다. 있는 번호를 바꾸거나 재사용하지 않는다.

## 5. 디버깅은 전부 로그로

판단이 일어나는 곳마다 **왜 그랬는지**를 남기고 레벨로 거른다. 형식은 하나다:

```
[tag][L] key=value key=value
```

| 레벨 | 무엇 |
|---|---|
| `T` Trace | 매 틱 값 (위치 · 잔량 · 거리). 켜면 프레임당 수십 줄 |
| `D` Debug | 판단과 전이 (왜 이 선택인지 · 상태 머신) |
| `I` Info | 유저가 보는 사건 (씬 전환 · 승패) |
| `W` Warn | 이상하지만 계속 간다 (데이터 누락 기본값) |
| `E` Error | **규칙 위반** (없는 id · 음수 값) |

**`[E]` 는 규칙 위반이다.** 헤드리스 판정이 `[E]` 한 줄을 실패로 본다 — 공짜 불변식이다.
"이상하지만 계속 가도 되는 것"은 `[W]` 다. 그 구분을 흐리면 게이트가 무의미해진다.

`[M]` 은 레벨이 아니라 **완료 표지**다 (`Log.Marker`). 로그 레벨과 무관하게 항상 찍힌다 —
`judge_headless` 가 이 줄 하나로 "끝까지 갔는가"를 판정하므로, 레벨에 걸리게 두면
`LOG_LEVEL=warn` 이 전부 통과인데도 거짓 실패를 낸다.

에러가 나면 **재현 → 로그 확인 → 고침** 순서로만 간다. 로그를 안 보고 고치지 않는다.

## 6. 눈으로 안 봐도 확인된다

| 명령 | 무엇을 본다 | Godot 필요 |
|---|---|---|
| `tools/build.sh test` | 규칙 — 초 단위 | 아니오 |
| `tools/build.sh check` | 포맷 · 빌드 · 규칙 · uid (커밋 게이트) | 아니오 |
| `tools/build.sh smoke` | 씬이 뜨는지 · 스크립트가 붙는지 · **C# 예외가 나는지** | 예 |

마지막 항목이 중요하다. C# 예외는 우리 로그 형식(`[tag][E]`)으로 안 찍히고 엔진의 `ERROR:` 블록으로만 나온다 —
그래서 `judge_headless` 가 그 블록도 따로 본다. **거기에 C# 예외 면제를 넣지 마라.**
한 번 면제를 만들면 그 자리의 진짜 버그도 같이 초록이 된다.

## 7. 파일이 커지면 나눈다

한 파일이 하는 일이 하나여야 한다. 주석 제외 400줄쯤에서 "이 파일이 뭘 하는가"를 한 문장으로
말할 수 없으면 경계가 잘못된 것이다.

## 8. 코드 스타일

`.editorconfig` 하나가 유일한 기준이고 `dotnet format` 이 그 파일을 읽는다.
분석기는 **성능·신뢰성 카테고리를 전부** 켜 뒀다. 개별 억제는 `.editorconfig` 한 곳에 **사유와 함께** 둔다.

## 9. 주석

- **무엇을** 하는지가 아니라 **왜 그렇게** 했는지를 적는다.
- 되돌리면 안 되는 결정에는 그 이유와 **실제로 밟은 사례**를 같이 남긴다.
- 낡은 주석은 없느니만 못하다. 코드를 고치면 그 위 주석도 같이 고친다.
